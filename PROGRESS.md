# PROGRESS.md

Running log of work in this repo. Newest entry on top. One entry per session/task — what was done, what's left.

## 2026-08-31 — Google Reviews outbound link + eyebrow unification (cont. 47, code)

- **What:** new shared `src/components/shared/GoogleReviewsLink/` (`tone` light/dark, `label` prop, official 4-colour Google "G" + `ArrowUpRight`, `rel="noopener noreferrer"`, 44px target, `:focus-visible`, sr-only new-tab notice) → outbound link to the live GBP review panel. Rendered on Home (`Testimonials.jsx`), `/nosotros` (`AboutPage.jsx`), 6 hubs + 3 landings (`TestimonialsSection.jsx`), and the footer Contacto column (`Footer.jsx`). Single `GOOGLE_REVIEWS_URL` constant in `siteMeta.js` (= `https://maps.app.goo.gl/Hgy4FgMVrEJoFWZWA`, validated live → DERMA.M listing 4.9). Eyebrow unified: `aboutPage.js` `RESEÑAS DE GOOGLE` → `GOOGLE REVIEWS` (all other surfaces already matched). Labels: sections carry the `GOOGLE REVIEWS` eyebrow so the link reads **"Ver más en Google"** (action, no translated echo); footer has no eyebrow + lists plain labels so it keeps **"Reseñas en Google"** (noun). No layout/grid change — mobile testimonials stay a vertical stack. NO `Review`/`aggregateRating` schema (8.18 policy risk stays avoided). Executes Part 1 of the 2026-08-29 reviews-alignment (8.20) spec.
- **Why:** the `GOOGLE REVIEWS` eyebrow claimed a provenance the page never let you verify. A followed outbound link to the real GBP panel is the honest trust mechanism instead of a curated-reviews `aggregateRating`. Audit 8.20 rescoped 2026-08-29 (no APIFY, no online integration).
- **Verified:** browser at 375px then desktop on Home, `/nosotros`, `/faciales`, `/corporales`, `/limpieza-facial-profunda` + footer — link visible, `height` 44, `rel`/`target` correct, `:focus-visible` outline, opens the correct GBP listing in a new tab. Contrast: light `#363633`/`#F2F0F1` ≈ 10.6:1 · dark `#CCC9C1`/`#141313` = 11.2:1 (AA). Console clean on every route. `git grep` guards: review URL only in `siteMeta.js` (+ pre-existing `Contacto.jsx`); zero `RESEÑAS DE GOOGLE` residual. `npm run test:visual` (shared component/template gate, run at `ce6a32f`): **33 passed, 1 failed = pre-existing `Nosotros Page - Viewport` desktop-chrome** (`about/hero.jpg` placeholder, documented). No new diffs, no baseline updates — snapshots capture neither the footer nor any testimonials block. `faq-consistency` 12/12. The 3 later copy commits (`cf129cc`, `e62a7db`) are one-component text, no CSS/layout, outside every snapshot region — no re-run. `MEDICAL_COMPLIANCE`: only new copy is "Ver más / Reseñas en Google", neutral. SEO/GEO/AEO: mild-positive trust/entity (followed link to real GBP panel corroborates the on-page quotes + NAP entity); nil ranking; consistency +minor (one eyebrow string).
- Commits `5ff5d05` (component + constant) · `5f9ca54` (Home) · `e1a924a` (/nosotros + eyebrow) · `b581722` (hubs + landings) · `0764a59` (footer) · `ce6a32f` (8.20 audit note) · `cf129cc` + `e62a7db` (label wording). Spec `docs/superpowers/specs/2026-08-31-google-reviews-outbound-link-design.md`, plan `docs/superpowers/plans/2026-08-31-google-reviews-outbound-link.md`. **8.20 Part 1 done; Parts 2 (per-page topical curation) + 3 (`aggregateRating` decision, needs clinic sign-off) remain backlog.**

---

**Earlier history:** see [docs/PROGRESS_ARCHIVE.md](docs/PROGRESS_ARCHIVE.md).
