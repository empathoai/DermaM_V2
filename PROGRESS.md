# PROGRESS.md

Running log of work in this repo. Newest entry on top. One entry per session/task — what was done, what's left.

## 2026-08-31 — Task 25 (CPY-07): ES headline sentence-case + titleCase acronym whitelist (cont. 44, code)

- **What:** (A) ~70 prose `<h2>` headlines ALL-CAPS → ES sentence case across `src/data/treatmentPages.js` (25 `problemContextHeadline` + 2 `beforeAfter.headline` + 2 JSX fallback literals; folded typos `LA PERDIDA`→`La pérdida`, `ACUMULO`→`Acúmulo`), `src/data/landingPages.js` (15), `src/data/categoryPages.js` (30). Short structural labels (`LO QUE DICEN NUESTROS CLIENTES`, `PREGUNTAS FRECUENTES`, `CÓMO ES TU VISITA`, `TRATAMIENTOS DE …`, `SUEROS IV THERAPY`, `TRATAMIENTO CAPILAR`) left ALL-CAPS. (B) `src/utils/text.js` — `titleCase()` now restores a fixed whitelist `HIFU IPL PRF PRP IV MS EVEFUS DERMAPEN` to uppercase after its word-cap pass (whole-word match, delimited by start/end/space/`/`); `DERMAPEN` forced uppercase. Header comment updated.
- **Why:** audit CPY-07. Re-scoped from "L — all `src/data/*` titles" to A+B after finding the ALL-CAPS `title:` storage is a deliberate `titleCase()`-fed convention (not touched). A: screen readers spell ALL-CAPS prose letter-by-letter + LLMs extract "shouting" `<h2>`s. B: `titleCase()` mangled acronyms/brands in `<title>`, OG/Twitter, JSON-LD `name`, `alt` ("Hifu", "Prf"). Spec `docs/superpowers/specs/2026-08-31-task25-title-casing-design.md`, plan `docs/superpowers/plans/2026-08-31-task25-title-casing.md`.
- **Verified:** `node -e` table for `titleCase()` (8 cases incl. `HIFU FACIAL`→`HIFU Facial`, `MICRONEEDLING / DERMAPEN`→`Microneedling / DERMAPEN`, `RECOVERY & PERFORMANCE IV` unchanged, empty string) — ALL PASS. Browser `:3000`: `/faciales/hifu-facial` — problem `<h2>` DOM text now sentence case, `getComputedStyle().textTransform === 'uppercase'` → **zero visual change**; `document.title` = `HIFU Facial en West Palm Beach | Derma.M` (was "Hifu"); JSON-LD `Service.name` = `HIFU Facial`. `/faciales` hub + `/prf-y-fibrina` landing — headlines sentence case, entity `Plasma Rico en Plaquetas y Fibrina` preserved, labels intact; no console errors; 375px no horizontal overflow. `grep` of added lines vs `MEDICAL_COMPLIANCE` banned words → none. `test:visual` **skipped, justified** (`CLAUDE.md` §DoD): no render change (CSS `text-transform` forces uppercase), no DOM in any snapshot viewport, `titleCase()` output feeds only `<title>`/meta/schema/`alt`. SEO/GEO/AEO: low-positive (correct entity casing in SERP `<title>` + schema; `<h2>`s stop shouting for LLM extraction); GEO none; a11y real gain.
- Commits `e39c374` (B / titleCase) · `172ba3e` (treatments) · `0c8af78` (landings) · `c82fab1` (hubs). **Task 25 closed.**

---

**Earlier history:** see [docs/PROGRESS_ARCHIVE.md](docs/PROGRESS_ARCHIVE.md).
