# DECISIONS.md

Record of non-obvious decisions and their rationale. Append new entries at the bottom; don't rewrite history — if a decision is reversed, add a new entry that supersedes it and link back.

## 2026-08-19 — Keep Codex global config untouched
**Decision:** Do not read from, write to, or rely on `~/.codex/config.toml` (or any other Codex-global config) as part of work in this project.
**Why:** User explicitly asked not to have Codex-level tooling (e.g. the TestSprite MCP server) considered in this project, to avoid mixing unrelated tool config into DermaM's scope.
**How to apply:** If a Codex/other-agent config file is discovered again, treat it as out of scope unless the user asks specifically to import from it.

## 2026-08-19 — graphify scoped to `src/` only, output at project root
**Decision:** Built the graphify knowledge graph from `src/` (code only), with `--out .` so `graphify-out/` lives at the project root, not nested under `src/`.
**Why:** Running `graphify extract .` over the whole repo pulled in 64 docs + 153 images and failed without an LLM API key (semantic extraction needed for non-code files). Scoping to `src/` with `--no-cluster` avoids that entirely since a code-only corpus needs no key. The skill's own instructions (`CLAUDE.md` → `## graphify`) assume `graphify-out/` at the project root, so `--out .` was needed to match that convention.
**How to apply:** Re-run `graphify update .` (not `extract`) after code changes — no API cost. Only re-run a full `extract` if the graph needs docs/images included, and only then decide whether to supply an LLM backend key.

## 2026-08-19 — Project treated as near-final; strict change discipline enforced
**Decision:** From this point on, no change happens without explicit user request and explicit approval, even after a plan is written via superpowers. No scope creep, no unrequested cleanup/refactors. Claude must push back on illogical or risky requests instead of agreeing by default. Work proceeds one requested change at a time — never several bundled together — and each approved change is closed out by updating PROGRESS.md/DECISIONS.md/MEMORY.md before starting the next.
**Why:** User stated the site is essentially finished ("esta casi terminado") and wants token/context-efficient sessions where each unit of work is fully registered before moving on, and wants to be actively stopped if a request doesn't make sense or risks damaging what already works — not agreed with by default.
**How to apply:** Rules are codified in `CLAUDE.md` under "Mandatory: project is near-final — protect it" and "Mandatory: one change at a time, close it out fully". Treat these as binding for every future session in this repo.

## 2026-08-19 — `ai-visibility-checker`'s "never simulate an AI engine response" rule kept even though the user asked for non-blocking heuristic mode everywhere
**Decision:** When adding "Modo Heurístico / Input Manual" fallbacks to the SEO skill suite (so no skill hard-blocks without the DataForSEO connector), `ai-visibility-checker` kept its rule that it must never fabricate what ChatGPT/Gemini/Perplexity/AI Overviews "probably" answered. Its non-blocking path is asking the user to paste real engine responses instead — if they decline and the connector isn't available, it still reports "couldn't verify" rather than inventing a citation result.
**Why:** A simulated AI-citation result is materially different from other heuristic estimates in this suite (keyword volume, backlink counts) — it's not a rough number, it's a fabricated claim about what a third-party AI system said, presented as if observed. That's the exact failure mode the skill's original design existed to prevent. Making it "not block" by having it guess would trade a real defect (blocking) for a worse one (confidently wrong output presented as data).
**How to apply:** If asked to make this skill fully non-blocking (i.e. remove the "ask user to paste real responses" requirement too), push back and flag this trade-off explicitly rather than complying silently — this is exactly the kind of illogical/risky request `CLAUDE.md`'s near-final protection rules ask to surface, not implement quietly.

## 2026-08-20 — Native `<details>`/`<summary>` for the Contacto legal accordion, not a custom toggle component
**Decision:** The collapsible "Aviso legal / Legal notice" block on `/contacto` uses plain HTML `<details>`/`<summary>` styled via CSS Modules, not a `useState`-driven custom component.
**Why:** Native `<details>` gives keyboard support (Tab, Enter/Space) and ARIA semantics for free — no JS state, no extra accessibility work, and it's the smallest change that satisfies the goal (shorter scroll, no copy changes). Matches the project's YAGNI stance for a near-final site.
**How to apply:** Reach for `<details>`/`<summary>` first for any other collapse/expand UI on this site (FAQ-style content, other long disclaimers) before building a custom accordion component — only deviate if a requirement native `<details>` can't satisfy comes up (e.g. animated height transitions).

## 2026-08-20 — AGENTS.md reduced to a pointer at CLAUDE.md, instead of fixed in place
**Decision:** Rather than repairing `AGENTS.md`'s dead `file:///d:/Derma_Content/...` links to point at the current checkout, its content was deleted and replaced with a 4-line pointer: "read CLAUDE.md first." `CLAUDE.md`'s own task-routing note was updated to stop treating `AGENTS.md` as an authoritative fallback doc.
**Why:** `AGENTS.md` was ~90% duplicate of `CLAUDE.md`'s rules, restated with different wording and absolute paths from a different machine/drive (`d:/Derma_Content/Website_DermaM_V2/derma.m/...`) that don't exist in this checkout (`f:\EmpathoAI-projects\DermaM_Website`). Fixing the links in place would have kept two sources of truth that drift again the next time either file is edited. `AGENTS.md` exists only because some non-Claude tools look for that filename by convention — it doesn't need its own rule content.
**How to apply:** Any future rule change belongs in `CLAUDE.md` only. Don't add rule content back into `AGENTS.md` — if a tool needs `AGENTS.md` to say more than "see CLAUDE.md," that's a signal to reconsider, not to duplicate.

## 2026-08-20 — CLAUDE.md's SEO suite section cut from a command table to workflow-order + gate rule only
**Decision:** Removed the 8-row Spanish command table (`/ai-seo`, `/seo-audit`, etc. → purpose) from `CLAUDE.md` and kept only the recommended diagnostic order and the "SEO output still needs brainstorming + approval" rule.
**Why:** Each skill's own `description` field is already surfaced to the agent every session via the harness's skill listing — it already functions as that skill's pointer. Restating "command → purpose" a second time in `CLAUDE.md` was duplicated content that would drift from the skill files (as it already had — old skill names lingered in `MEMORY.md`/`PROGRESS.md` history after a suite swap). The workflow order and the approval-gate rule are the only two things the skill listing doesn't already carry, so those are what's kept.
**How to apply:** When a 9th SEO skill is added or one is renamed, update the skill's own `SKILL.md` description — don't add a new table row to `CLAUDE.md`.

## 2026-08-20 — Replaced the sanitized ad-hoc SEO suite with the vault's "canonical-suite" wholesale, instead of patching it further
**Decision:** Rather than continuing to sanitize/patch the previous 11-skill suite (`seo`, `seo-local`, `seo-audit`, `seo-content`, plus the 5-step pipeline), deleted it entirely and installed a different 8-skill suite (`ai-seo`, `seo-audit`, `seo-local`, `seo-checklist-65`, `schema`, `site-architecture`, `programmatic-seo`, `cro`) from a separate `canonical-suite/` folder in the same source vault, per explicit user instruction.
**Why:** User stated the previous suite was a "reseteo limpio" target and explicitly named the `canonical-suite/` path as "100% Sanitizada y Verificada" — a different, already-clean origin rather than the "Palo Seco" fragments patched on 2026-08-19. This is a full swap, not an incremental fix, so the old suite's saneamiento notes (in `PROGRESS.md`/`MEMORY.md`) are now historical only.
**How to apply:** Don't try to reconcile or merge the two suites' skill names (e.g. old `seo-content` vs new `ai-seo`/`schema`) — they're different tools with different scopes. If old skill names (`seo-content-writer`, `keyword-fanout-map`, etc.) are referenced again, treat them as removed, not renamed.

## 2026-08-20 — New team member (Dr. Miguel Ramos) added without photo/video asset, using existing MediaBlock fallback instead of a placeholder image
**Decision:** Added the Dr. Miguel Ramos "Estética Dental" team entry to `src/data/aboutPage.js` with `mediaSrc`/`videoSrc` left `undefined`, rather than waiting for the asset or wiring in a stock/placeholder image.
**Why:** `src/components/shared/MediaBlock/MediaBlock.jsx` already has a built-in fallback (`if (!src || error) return <div className={fallback...} />`) that renders a clean, on-brand empty block when no `src` is given — this is existing, tested behavior used elsewhere in the codebase, not new code. Using it avoids inventing a placeholder asset or leaving a broken `<img>` tag, and requires zero component changes.
**How to apply:** When a new team member/media-driven card is requested without a ready asset, prefer leaving `mediaSrc` undefined over supplying a stand-in image, as long as the consuming component already has a fallback path like `MediaBlock`. Verify the fallback exists by reading the component before relying on it (done here for `TeamMemberCard.jsx` → `MediaBlock.jsx`).

## 2026-08-20 — Miguel Ramos's vCard uses the shared clinic phone, matching every other team member (not a bug)
**Decision:** `public/team/vcards/miguel-ramos.vcf` was created with the same `TEL;TYPE=CELL:+15612535384` as all 8 existing team vCards, rather than a personal number.
**Why:** User flagged this as a suspected bug ("el numero es el mismo de todos... validalo"). Reading `mikaela-guajardo.vcf` and `tony-diaz.vcf` confirmed every existing vCard shares the identical clinic phone and only varies `FN`/`TITLE` — this is the established intentional pattern (one shared front-desk/clinic number for all staff vCards), not a copy-paste bug. Matched it for consistency rather than introducing the first personal-number exception.

## 2026-08-20 — SEO 8.1 fix (canonical duplicado): removed the computed `VITE_SITE_URL`-based canonical entirely rather than fixing its fallback domain
**Decision:** For the 6 pages with a duplicate `<link rel="canonical">` (Contacto + 5 treatment templates), deleted the `siteUrl`/`canonicalUrl` computed-from-env variables and their conditional `<link>` outright, keeping only the pre-existing hardcoded `dermamskinhealth.com` canonical — instead of fixing the wrong fallback domain (`https://dermam.com`, backlog item 8.2) and keeping the computed version as the single source.
**Why:** The hardcoded tag was already correct and present in all 6 files; the computed one was redundant duplication whose only distinguishing behavior was a wrong fallback domain when `VITE_SITE_URL` is unset. Removing the computed path fixes 8.1 (duplication) without introducing new env-var-dependent behavior on a near-final site, and it makes item 8.2 (fallback domain) moot for these 6 files since the code that caused it no longer exists.
**How to apply:** Item 8.2 in `docs/SEO_AUDIT_2026.md` still needs an explicit close-out decision — confirm no other file still reads `VITE_SITE_URL` for canonical construction before marking it `Hecho`/resolved-as-duplicate-of-8.1.
**How to apply:** If asked to add a personal phone number to any team member's vCard in the future, treat that as a deliberate deviation from the established pattern and flag it back to the user before applying, rather than assuming it's a fix.

## 2026-08-20 — Installed `bencium-aeo` + `keyword-research` skills to supplement the wiki, instead of inventing a keyword strategy
**Decision:** During the SEO/GEO/AEO audit, asked to design a query-identification strategy for the 3 featured landing pages. No such methodology existed in the SecondBrain wiki or in `seo-checklist-65`. Instead of drafting one from scratch, searched (via `find-skills`/`npx skills find`) for an existing external skill, found `bencium/bencium-marketplace@bencium-aeo` (2.5K installs) and `kostja94/marketing-skills@keyword-research` (1.1K installs) as the most-installed candidates in their categories, confirmed with the user, and installed both to `.agents/skills/`.
**Why:** The wiki explains *why* GEO/AEO matters and the high-level pillars, but has no tactical mechanics for finding real target queries or structuring AEO content. Rather than improvise a first-draft methodology, a vetted, widely-adopted external skill was preferred once one was found — consistent with "avoid proposing new code/process when suitable implementations already exist."
**How to apply:** The wiki (`F:\OS-EmpathoAI-SecondBrain`) stays read-only per `CLAUDE.md` and is not to be edited from this project even to add this finding — the record of this decision and the applied methodology lives in `docs/SEO_AUDIT_2026.md` (section 3) and `MEMORY.md` instead. If more SEO/marketing tactical gaps surface, check `find-skills` before drafting an in-house methodology.

## 2026-08-20 — docs/MEDICAL_COMPLIANCE.md sourced from the user's older local checkout (`D:\Derma_Content\...`), not the SecondBrain wiki's Florida compliance matrix
**Decision:** When closing SEO audit item 4.1 (missing `docs/MEDICAL_COMPLIANCE.md`), used the file the user pointed to at `D:\Derma_Content\Website_DermaM_V2\derma.m\docs\MEDICAL_COMPLIANCE.md` (copied verbatim), instead of the per-treatment compliance matrix found in the SecondBrain wiki (`wiki/operations/florida-healthcare-beauty-advertising-compliance.md`).
**Why:** The wiki matrix only covers 3 of the site's ~25 actual treatments (microneedling, laser hair removal, IPL) — the rest of its rows are Botox/fillers/Kybella/PMU/nail services that Derma.M does not offer, confirmed by cross-checking `public/sitemap.xml`. Using it as the compliance source would have left ~22 real treatments uncovered. The `D:\` file is a general, site-wide (not per-treatment) rules doc the user wrote through their own research — smaller in scope but complete for its purpose, and the correct one per explicit user confirmation ("ese documento lo armé yo... no me lo inventé").
**How to apply:** Don't treat the wiki's Florida matrix as the compliance source of truth for this site — it's a broader industry reference (med spas generally), not scoped to Derma.M's actual service list. If per-treatment claim specifics are ever needed (e.g. for the 8.4 AEO content initiative), cross-check any external compliance matrix against `public/sitemap.xml` / `src/data/treatmentPages.js` first to confirm coverage before relying on it.

**Update (same day):** the per-service *table* stays excluded for the reason above, but the user asked to add the matrix's *general* (non-per-treatment) sections as a complement — Florida legal/operational rules (physician delegation, medical director requirement, informed consent, record retention), the FTC claims-evidence framework, and privacy rules (HIPAA/FIPA/Meta CAPI). These apply regardless of which specific treatment is being discussed, so the coverage-gap objection doesn't apply to them. Added verbatim as a separate "Florida Legal & Operational Rules (complementary reference)" section in `docs/MEDICAL_COMPLIANCE.md`, clearly labeled as complementary to (not replacing) the original 8 copy-wording rules.

## 2026-08-20 — SEO backlog priorization (8.4 vs 8.7 vs 2.3 vs redirects) run through `/llm-council`
**Decision:** Of the four remaining SEO backlog items, tackled 8.7 (geo/openingHoursSpecification) first, not 8.4 (JSON-LD on 3 landing pages) despite 8.4's larger AEO/AI-citation upside. 2.3 (hreflang ES/EN) was explicitly not touched — not even the "decide" half — until it's confirmed whether an EN site actually exists in full or only a handful of legal-page aliases. The 301 redirect audit stays deferred to pre-launch, unchanged.
**Why:** A 5-advisor council (Contrarian, First Principles, Expansionist, Outsider, Executor) plus a peer-review pass converged 4-to-1 on 8.7 first: it closes out work already started (`aggregateRating` was added in a prior session) with the smallest possible blast radius (pure data edit, no routing, no new components) — consistent with this project's "one item at a time, close it out fully" rule. 8.4 was flagged as actually bundling 3 separate schema-type decisions across 3 pages, which itself risks violating the same rule. 2.3 was flagged as combining "decide" and "implement" under one backlog label — implementing hreflang without a verified 1:1 ES/EN URL map is one of the most damaging Search Console errors (dangling hreflang references), so even the "decide" step needs an answer to "does the EN site actually exist?" before proceeding.
**How to apply:** Before writing 8.7's `geo`/`openingHoursSpecification`, verified coordinates against the existing Google Maps iframe already embedded in `Contacto.jsx` (rather than geocoding blind) and got explicit user confirmation that the existing `aggregateRating` (4.9★/117) is backed by real, verifiable reviews — the council's peer-review round flagged unverified aggregateRating as a "schema spam" risk under Google's guidelines. Apply the same pattern (verify against an existing source of truth already in the codebase, don't write operational/factual schema data from assumption) to 8.4 and any future structured-data work. Next up: 8.4, in its own session, through `superpowers:brainstorming` first and cross-checked against `docs/MEDICAL_COMPLIANCE.md` since it touches clinical-claim-adjacent schema (FAQPage/Service/MedicalProcedure). 2.3 needs the EN-site-completeness question answered before it's even scheduled.

## 2026-08-20 — No license numbers/credential IDs on the new Nancy Nieto bio page (item 4.2), by explicit instruction from Nancy directly
**Decision:** The `/nosotros/nancy-nieto` page and its JSON-LD `Person` schema use only qualitative credential language already present in `aboutPage.js` ("Flebotomista certificada en Estados Unidos", "especialista facial con licencia en Florida"). No license number, no `hasCredential` schema property, no new unverified credential data was added, even though a dedicated bio page is normally the place E-E-A-T guidance recommends putting exactly that kind of verifiable detail.
**Why:** User relayed Nancy's explicit concern: Florida has a known pattern of unlicensed operators appropriating a licensed med-spa's credentials to purchase products/services they aren't authorized to buy. Publishing a verifiable license number is a real operational risk for the practice, not a style preference — it overrides the general SEO best-practice of maximizing credential transparency on author-bio pages.
**How to apply:** Do not add license numbers, license IDs, or `schema.org` `hasCredential`/`credentialCategory` fields to any Derma.M staff bio (this page, the `/nosotros` team cards, or any future per-provider page for Tony Díaz DO / Dr. Miguel Ramos) unless Nancy or the user explicitly reverses this instruction. If asked to "improve E-E-A-T signals" in the future, flag this constraint rather than adding license data by default.

## 2026-08-20 — `Picture.jsx`'s `<picture>` element forced to `display: contents` rather than styling it with the consumer's className
**Decision:** Fixed the FeaturedServices image-gap bug (see `PROGRESS.md`) by adding `style={{ display: 'contents' }}` to the `<picture>` wrapper inside `src/components/shared/Picture/Picture.jsx`, instead of also passing `className`/`style` down onto `<picture>` itself (e.g. `<picture className={className}>`).
**Why:** `display: contents` removes `<picture>` from the box-generation tree entirely, so the `<img>` becomes the effective flex/grid child of whatever the consumer's container is — this exactly reproduces the pre-migration markup (a bare `<img>` with no wrapper) that every consumer's existing CSS (`FeaturedServices.module.css`, `AboutPage.module.css`, `MediaBlock.module.css`, etc.) was already written for. Passing `className` to `<picture>` instead would have required every consumer's CSS to be checked/duplicated for two class targets (`<picture>` and `<img>`) and could have caused double-styling bugs (e.g. two elements both getting `width:100%; height:100%` in nested boxes with different box models). `display:contents` is a single, universal, zero-config fix that doesn't require touching any of the 7 consumers.
**How to apply:** Keep `Picture.jsx`'s `<picture>` wrapper as `display:contents` — don't remove it or start passing sizing props to `<picture>` directly. If a future browser-compat concern rules out `display:contents` (rare — it's been supported in all evergreen browsers for years), the fallback is to move `className`/`style` onto `<picture>` AND audit every consumer's CSS for the two-element case, not a quick patch.

## 2026-08-20 — SEO backlog item 2.3 closed: no `hreflang` needed, no EN site exists
**Decision:** Did not implement `hreflang` tags for the ES/EN route pairs (privacy, terms, disclaimer). Closed 2.3 as "no aplica" rather than "pendiente".
**Why:** User confirmed there is no English site — the EN-language route aliases are legal-page duplicates only, not a real translated site, and they're already in `Disallow` in `robots.txt` (never indexed). `hreflang` exists to tell search engines about equivalent pages in other languages; with no real EN site and the aliases blocked from indexing, adding it would create dangling/self-referencing hreflang entries for no benefit — a known Search Console error class this project deliberately avoided (see the 8.7 decision above on not proceeding without verifying a "does X actually exist" precondition).
**How to apply:** If a real EN site is ever built (not just legal-page aliases) and gets un-blocked from `robots.txt`, revisit hreflang then. Until that happens, treat 2.3 as closed, not blocked.

## 2026-08-20 — SEO backlog item 5.4 closed without implementing: font `preconnect`/`preload`/`font-display` deferred
**Decision:** Did not add `<link rel="preconnect">` for Google Fonts, nor move the `Poppins` load from CSS `@import` to a `<link>` in `index.html`. Closed 5.4 as "no se implementa por ahora" instead of leaving it open indefinitely.
**Why:** Ran a 5-advisor council (Contrarian, First Principles, Expansionist, Outsider, Executor) plus peer review on whether to make this change. Key finding: `&display=swap` is already present in the existing `@import`, so the FOIT problem this ticket exists to solve is already solved — the only remaining upside is an unmeasured, likely-millisecond DNS/TLS discovery-latency shave. Weighed against touching `index.html` (a shared, load-order-sensitive file) on a project explicitly frozen for stability with a visual-regression suite that already carries 9 pre-existing unrelated failures (degraded signal-to-noise for catching a new one), the risk/reward didn't clear the bar for a "near-final, protect what works" project. User confirmed: leaving it as-is has no negative impact (no broken FOIT, no measurable SEO penalty) — it's a "nice to have," not a fix for something broken.
**How to apply:** Do not re-open 5.4 speculatively. Revisit only if real performance metrics (Lighthouse/PageSpeed/Core Web Vitals data, not assumption) show font-loading latency is an actual measured problem — then the lowest-risk version to implement is `<link rel="preconnect">` tags added *alongside* the existing `@import` (not replacing it), which the council flagged as capturing most of the benefit with near-zero risk to the CSS cascade or Playwright snapshots. Do not bundle in font self-hosting, subsetting, or `font-display:optional` — those came up during the council as scope-creep tangents, not part of this ticket.

## 2026-08-26 — Toda imagen nueva servida por `Picture.jsx` necesita su par `.webp` en disco, sí o sí
**Decision:** Al cablear las fotos antes/después de `/limpieza-facial-profunda`, además de subir los `.jpg` se generaron los `.webp` hermanos (`sharp` q78). No es una optimización opcional: es requisito para que la imagen se vea.
**Why:** `Picture.jsx` siempre emite `<picture><source srcset="<misma-ruta>.webp" type="image/webp"><img src="<ruta>.jpg"></picture>`. Cuando un `<source>` que hace match (el navegador soporta WebP) apunta a un recurso que da 404, el navegador **no** reintenta con el `<img>` de fallback — el fallback de `<picture>` es para cuando ningún `<source>` matchea por `type`/`media`, no para cuando el recurso elegido falla al descargar. Resultado: `naturalWidth=0` → dispara el `onError` de `BeforeAfterGrid` → `display:none` → placeholder negro. Subir solo el `.jpg` deja la imagen rota en todo navegador moderno.
**How to apply:** Cada vez que se agregue **o se reemplace** una imagen en `public/assets/images/**` que se renderice vía `Picture` (o `BeforeAfterGrid`, `MediaBlock`, `FeaturedServices`, etc.), (re)generar el `.webp` con el mismo basename en la misma carpeta. El navegador prefiere el `<source>.webp` sobre el `<img>.jpg`, así que un `.webp` viejo enmascara un `.jpg` nuevo (pasó en esta misma tarea: el usuario re-subió los jpg con otro encuadre y el sitio siguió mostrando el webp recortado anterior). Convención de la carpeta `landings/*`: q78, mismas dimensiones que el jpg. Comando: `node -e "require('sharp')('<f>.jpg').webp({quality:78}).toFile('<f>.webp')"`.

## 2026-08-26 — `BeforeAfterGrid` con labels configurables + override `beforeAfter` por tratamiento (enfoque A)
**Decision:** Para meter las 2 fotos dentales de `blanqueamiento-dental` en los 2 slots del `BeforeAfterGrid` con labels distintos ("PROCEDIMIENTO" / "ANTES Y DESPUÉS"), se agregaron props opcionales `beforeLabel`/`afterLabel` (default `'ANTES'`/`'DESPUÉS'`) al componente compartido y un override `data.beforeAfter` que `TreatmentDetailPage` consume vía `?? [ruta generada]`. El `alt` pasó a `item.beforeAlt || 'Before'` / `item.afterAlt || 'After'`. Los datos del override viven en `customDetails['blanqueamiento-dental'].beforeAfter` y se cablean al objeto compilado con `beforeAfter: custom.beforeAfter || null`.
**Why:** Descartados: (1) labels por item — YAGNI, el grid siempre recibe 1 solo item; (2) sistema de labels global para todo el sitio — sobre-ingeniería, ninguna landing necesita labels custom. El enfoque elegido es retrocompatible por construcción (props `undefined` → defaults; landings no pasan `beforeAlt`/`afterAlt` → alt sin cambios), toca 3 archivos y afecta un único tratamiento. Verificado que `/limpieza-facial-profunda` (landing) y `/dental-estetico/limpieza-dental` (tratamiento sin override) quedan idénticos.
**How to apply:** Para dar nombres SEO + labels custom a otro tratamiento, agregar `beforeAfter: { items: [{ before, after, beforeAlt, afterAlt }], beforeLabel, afterLabel, disclaimer? }` en su entrada de `customDetails`. No hace falta tocar el componente ni el template otra vez. El bug `categoryFolder` de `TreatmentDetailPage.jsx:59` (no mapea `dentalEstetico`) sigue vivo para tratamientos dentales SIN override — está en backlog junto con "ocultar la sección cuando no hay imágenes".

## 2026-08-27 — `RelatedTreatments` fuerza `columns={3}` siempre (no `items.length`)
**Decision:** `RelatedTreatments.jsx` pasa `columns={3}` fijo a `TreatmentGrid`, en vez de `columns={items.length >= 3 ? 3 : items.length}`.
**Why:** `TreatmentGrid.module.css` solo define `.cols2` (flex-basis 50%) y `.cols3` (33%). No hay `.cols1`. Cuando una categoría tiene <3 tratamientos (`dentalEstetico` y `laserYLuz`, 2 cada una), la lista de relacionados de un tratamiento queda con 1 item → `columns={1}` → clase `cols1` sin regla → la card sin `flex-basis` se estira a todo el contenedor → imagen `aspect-ratio: 1/1` gigante (hasta 1315px). Bug preexistente desde la creación del archivo (commit `34bdffd`, jun 2026), quedó expuesto al reducir tratamientos por categoría. Con `columns={3}` toda card de "Te puede interesar" usa `.cols3` (tamaño estándar ~426px) y el `justify-content: center` de `.grid` centra 1 o 2 cards. Faciales/corporales ya pasaban ≥3 → sin cambio (verificado en `/faciales/hidrofacial`: 3×426px intactas).
**How to apply:** No volver a atar `columns` a `items.length` en `RelatedTreatments`. Si algún día se agrega `.cols1` al CSS de `TreatmentGrid`, reconsiderar; hasta entonces `columns={3}` es lo correcto para esta sección. Las cards quedan centradas — si se pide alineadas a la izquierda, es un ajuste en `.grid` de `TreatmentGrid.module.css` (afecta todas las grillas que lo usan).

## 2026-08-27 — `imagePosition` de `categoryPages.js` cableado también a `RelatedTreatments` (una sola fuente)
**Decision:** El `imagePosition` que hasta ahora solo consumía `CategoryPage` (cards del hub) ahora también fluye a las cards de "Te puede interesar": `getBaseTreatment()` en `treatmentPages.js` devuelve `imagePosition: found.imagePosition`, el array `related` lo incluye, y `RelatedTreatments.jsx` lo pasa en su `items.map`. `TreatmentGrid` ya spreadea el item a `TreatmentCard`. No se agregó ningún valor nuevo: las 2 entradas dentales en `categoryPages.js` ya tenían `imagePosition: '74% center'`.
**Why:** El problema de encuadre (heros dentales con el sujeto a la derecha, recorte cuadrado centrado mostrando pared) aplica igual al hub y a "Te puede interesar" — mismo hero, mismo crop. Descartado un lookup hardcodeado `{slug: posición}` en `RelatedTreatments` (feo, no escala, deja el hub inconsistente con su propio "Te puede interesar") y CSS por categoría (`TreatmentCard` no emite clase de categoría, saltea el mecanismo `imagePosition` que ya existe desde commit `ecbff32`). Una sola fuente en `categoryPages.js` es lo más limpio.
**How to apply:** Para ajustar el encuadre de la card de un tratamiento (en cualquiera de los dos contextos), editar `imagePosition` en su entrada de `categoryPages.js` `featuredTreatments` — afecta hub y "Te puede interesar" a la vez. Efecto de este cambio: las cards de "Te puede interesar" de corporales empezaron a honrar su `imagePosition` (antes lo ignoraban) — verificado como mejora de consistencia, no regresión. Faciales/capilar sin `imagePosition` siguen en `center`.
