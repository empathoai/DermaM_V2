# DECISIONS.md

Record of non-obvious decisions and their rationale. Append new entries at the bottom; don't rewrite history — if a decision is reversed, add a new entry that supersedes it and link back.

## 2026-08-20 — No license numbers/credential IDs on Derma.M staff bios (Nancy, item 4.2)
**Decision:** The `/nosotros/nancy-nieto` page and its JSON-LD `Person` schema use only qualitative credential language already present in `aboutPage.js` ("Flebotomista certificada en Estados Unidos", "especialista facial con licencia en Florida"). No license number, no `hasCredential` schema property, no new unverified credential data was added, even though a dedicated bio page is normally the place E-E-A-T guidance recommends putting exactly that kind of verifiable detail.
**Why:** User relayed Nancy's explicit concern: Florida has a known pattern of unlicensed operators appropriating a licensed med-spa's credentials to purchase products/services they aren't authorized to buy. Publishing a verifiable license number is a real operational risk for the practice, not a style preference — it overrides the general SEO best-practice of maximizing credential transparency on author-bio pages.
**How to apply:** Do not add license numbers, license IDs, or `schema.org` `hasCredential`/`credentialCategory` fields to any Derma.M staff bio (this page, the `/nosotros` team cards, or any future per-provider page for Tony Díaz DO / Dr. Miguel Ramos) unless Nancy or the user explicitly reverses this instruction. If asked to "improve E-E-A-T signals" in the future, flag this constraint rather than adding license data by default.

> Entradas anteriores (2026-08-19 / 2026-08-20) archivadas en [docs/DECISIONS_ARCHIVE.md](docs/DECISIONS_ARCHIVE.md).

## 2026-08-26 — Toda imagen nueva servida por `Picture.jsx` necesita su par `.webp` en disco, sí o sí
**Decision:** Al cablear las fotos antes/después de `/limpieza-facial-profunda`, además de subir los `.jpg` se generaron los `.webp` hermanos (`sharp` q78). No es una optimización opcional: es requisito para que la imagen se vea.
**Why:** `Picture.jsx` siempre emite `<picture><source srcset="<misma-ruta>.webp" type="image/webp"><img src="<ruta>.jpg"></picture>`. Cuando un `<source>` que hace match (el navegador soporta WebP) apunta a un recurso que da 404, el navegador **no** reintenta con el `<img>` de fallback — el fallback de `<picture>` es para cuando ningún `<source>` matchea por `type`/`media`, no para cuando el recurso elegido falla al descargar. Resultado: `naturalWidth=0` → dispara el `onError` de `BeforeAfterGrid` → `display:none` → placeholder negro. Subir solo el `.jpg` deja la imagen rota en todo navegador moderno.
**How to apply:** Cada vez que se agregue **o se reemplace** una imagen en `public/assets/images/**` que se renderice vía `Picture` (o `BeforeAfterGrid`, `MediaBlock`, `FeaturedServices`, etc.), (re)generar el `.webp` con el mismo basename en la misma carpeta. El navegador prefiere el `<source>.webp` sobre el `<img>.jpg`, así que un `.webp` viejo enmascara un `.jpg` nuevo (pasó en esta misma tarea: el usuario re-subió los jpg con otro encuadre y el sitio siguió mostrando el webp recortado anterior). Convención de la carpeta `landings/*`: q78, mismas dimensiones que el jpg. Comando: `node -e "require('sharp')('<f>.jpg').webp({quality:78}).toFile('<f>.webp')"`.

## 2026-08-26 — `BeforeAfterGrid` con labels configurables + override `beforeAfter` por tratamiento (enfoque A)
**Decision:** Para meter las 2 fotos dentales de `blanqueamiento-dental` en los 2 slots del `BeforeAfterGrid` con labels distintos ("PROCEDIMIENTO" / "ANTES Y DESPUÉS"), se agregaron props opcionales `beforeLabel`/`afterLabel` (default `'ANTES'`/`'DESPUÉS'`) al componente compartido y un override `data.beforeAfter` que `TreatmentDetailPage` consume vía `?? [ruta generada]`. El `alt` pasó a `item.beforeAlt || 'Before'` / `item.afterAlt || 'After'`. Los datos del override viven en `customDetails['blanqueamiento-dental'].beforeAfter` y se cablean al objeto compilado con `beforeAfter: custom.beforeAfter || null`.
**Why:** Descartados: (1) labels por item — YAGNI, el grid siempre recibe 1 solo item; (2) sistema de labels global para todo el sitio — sobre-ingeniería, ninguna landing necesita labels custom. El enfoque elegido es retrocompatible por construcción (props `undefined` → defaults; landings no pasan `beforeAlt`/`afterAlt` → alt sin cambios), toca 3 archivos y afecta un único tratamiento. Verificado que `/limpieza-facial-profunda` (landing) y `/dental-estetico/limpieza-dental` (tratamiento sin override) quedan idénticos.
**How to apply:** Para dar nombres SEO + labels custom a otro tratamiento, agregar `beforeAfter: { items: [{ before, after, beforeAlt, afterAlt }], beforeLabel, afterLabel, disclaimer? }` en su entrada de `customDetails`. No hace falta tocar el componente ni el template otra vez. El bug `categoryFolder` de `TreatmentDetailPage.jsx:59` (no mapea `dentalEstetico`) sigue vivo para tratamientos dentales SIN override — está en backlog junto con "ocultar la sección cuando no hay imágenes".

## 2026-08-27 — `RelatedTreatments` fuerza `columns={3}` siempre (no `items.length`)
**Decision:** `RelatedTreatments.jsx` pasa `columns={3}` fijo a `TreatmentGrid`, en vez de `columns={items.length >= 3 ? 3 : items.length}`.
**Why:** `TreatmentGrid.module.css` solo define `.cols2` (flex-basis 50%) y `.cols3` (33%). No hay `.cols1`. Cuando una categoría tiene <3 tratamientos (`dentalEstetico` y `laserYLuz`, 2 cada una), la lista de relacionados de un tratamiento queda con 1 item → `columns={1}` → clase `cols1` sin regla → la card sin `flex-basis` se estira a todo el contenedor → imagen `aspect-ratio: 1/1` gigante (hasta 1315px). Bug preexistente desde la creación del archivo (commit `34bdffd`, jun 2026), quedó expuesto al reducir tratamientos por categoría. Con `columns={3}` toda card de "Te puede interesar" usa `.cols3` (tamaño estándar ~426px) y el `justify-content: center` de `.grid` centra 1 o 2 cards. Faciales/corporales ya pasaban ≥3 → sin cambio (verificado en `/faciales/hidrofacial`: 3×426px intactas).
**How to apply:** No volver a atar `columns` a `items.length` en `RelatedTreatments`. Si algún día se agrega `.cols1` al CSS de `TreatmentGrid`, reconsiderar; hasta entonces `columns={3}` es lo correcto para esta sección. Las cards quedan centradas — si se pide alineadas a la izquierda, es un ajuste en `.grid` de `TreatmentGrid.module.css` (afecta todas las grillas que lo usan).

## 2026-08-27 — `imagePosition` de `categoryPages.js` cableado también a `RelatedTreatments` (una sola fuente)
**Decision:** El `imagePosition` que hasta ahora solo consumía `CategoryPage` (cards del hub) ahora también fluye a las cards de "Te puede interesar": `getBaseTreatment()` en `treatmentPages.js` devuelve `imagePosition: found.imagePosition`, el array `related` lo incluye, y `RelatedTreatments.jsx` lo pasa en su `items.map`. `TreatmentGrid` ya spreadea el item a `TreatmentCard`. No se agregó ningún valor nuevo: las 2 entradas dentales en `categoryPages.js` ya tenían `imagePosition: '74% center'`.
**Why:** El problema de encuadre (heros dentales con el sujeto a la derecha, recorte cuadrado centrado mostrando pared) aplica igual al hub y a "Te puede interesar" — mismo hero, mismo crop. Descartado un lookup hardcodeado `{slug: posición}` en `RelatedTreatments` (feo, no escala, deja el hub inconsistente con su propio "Te puede interesar") y CSS por categoría (`TreatmentCard` no emite clase de categoría, saltea el mecanismo `imagePosition` que ya existe desde commit `ecbff32`). Una sola fuente en `categoryPages.js` es lo más limpio.
**How to apply:** Para ajustar el encuadre de la card de un tratamiento (en cualquiera de los dos contextos), editar `imagePosition` en su entrada de `categoryPages.js` `featuredTreatments` — afecta hub y "Te puede interesar" a la vez. Efecto de este cambio: las cards de "Te puede interesar" de corporales empezaron a honrar su `imagePosition` (antes lo ignoraban) — verificado como mejora de consistencia, no regresión. Faciales/capilar sin `imagePosition` siguen en `center`.

## 2026-08-27 — `BeforeAfterGrid` renderiza `<video>` para src `.mp4` (branch por extensión, sin prop nueva)
**Decision:** `BeforeAfterGrid` detecta `.mp4` en `item.before`/`item.after` (helper `isVideo`) y renderiza `<video autoPlay muted loop playsInline preload="metadata" poster={src.replace('.mp4','.jpg')} aria-label={alt}>` en vez de `<Picture>`. Se factorizó un sub-render `SlotMedia` para no triplicar el JSX. `fallbackAlt` explícito (`"Before"`/`"After"`) preserva el default previo.
**Why:** La landing `prf-y-fibrina` no tiene antes/después — tiene un video de procedimiento + una imagen de resultado, y el usuario quiere los dos en los 2 slots 4:5 del mismo bloque. Descartado un componente nuevo `ResultShowcase`: duplicaría el 90% de `BeforeAfterGrid` (grilla 4:5, labels configurables, disclaimer) por una sola landing. El branch por extensión es contenido — solo se activa con un src `.mp4`, y hoy ningún otro caller (landings ni tratamientos) pasa uno, así que el resto no cambia (verificado). Patrón de `<video>` tomado de `HeroMedia.jsx` (poster derivado del `.jpg` homónimo) + `loop` (clip corto de resultados).
**How to apply:** Para poner un video en un slot de `BeforeAfterGrid` (cualquier landing o tratamiento vía su `beforeAfter`/`data.beforeAfter`), poner la ruta `.mp4` en `before`/`after` y tener un `.jpg` con el mismo basename al lado (poster). `LandingPage` ya pasa `beforeLabel`/`afterLabel` (se agregó en este cambio; `TreatmentDetailPage` ya lo hacía). El sitio no honra `prefers-reduced-motion` en ningún autoplay (hero, team, ahora este) — si se decide honrarlo, hacerlo en un pase transversal, no solo acá.

## 2026-08-27 — Transcodificación de video para web: base de `assets-optimizer` + `yuv420p` + `faststart` + downscale al slot
**Decision:** El mp4 de `prf-y-fibrina` (HEVC 10-bit-ish de teléfono, 15.8 MB, 1080×1440) se transcodificó con `ffmpeg -vcodec libx264 -crf 28 -an -preset slow -pix_fmt yuv420p -movflags +faststart -vf scale=900:-2` → H.264 900×1200, 2.1 MB. No se usó el script `assets-optimizer/optimize.js` tal cual.
**Why:** El script de la skill (`-vcodec libx264 -crf 28 -an -preset slow`) es la base correcta pero le faltan 3 cosas para video de teléfono en web: (1) `-pix_fmt yuv420p` — HEVC de iPhone suele ser 10-bit / formato que H.264 web no reproduce en varios navegadores; (2) `-movflags +faststart` — mueve el moov atom al principio para que reproduzca antes de descargar completo (streaming progresivo, clave con autoplay); (3) downscale a 900px — el slot muestra ~650px, 1080 es sobredimensionado; 900 = ~1.4x retina y bajó de 2.87 MB a 2.1 MB. HEVC además tiene soporte parcial en Chrome/Firefox, así que transcodificar a H.264 no era opcional.
**How to apply:** Para cualquier video nuevo destinado a `<video>` en el sitio, usar ese comando (ajustar `scale=` al ancho de display real ×1.5 aprox.). Correr la skill `assets-optimizer` primero si se quiere, pero verificar `codec_name` (debe quedar `h264`), `pix_fmt` (`yuv420p`) y que el moov esté al frente. Guardar el original antes de reemplazar in-place. Target de peso: < 3 MB, idealmente ~2 MB para 4:5 de ~15s.

## 2026-08-27 — Nombre público canónico "Plasma Rico en Plaquetas y Fibrina" (backlog 8.9/8.10)
**Decision:** En todo lugar de nombre público (nav ×2, footer, `<h3>` de FeaturedServices, `title` de card en `categoryPages.js`, headline de CTA, `name` en BreadcrumbList schema, `<title>`/`og:title`/`twitter:title`, fallback de `description` de schema, y 2 preguntas de FAQ que nombran el tratamiento) se usa **"Plasma Rico en Plaquetas y Fibrina"**. Las siglas "PRP"/"PRF" quedan **solo** en las FAQ que explican la diferencia entre ambas. El `<title>` es `Plasma Rico en Plaquetas y Fibrina | Derma.M` (sin "en West Palm Beach" para no pasar ~60 caracteres). El slug `/prf-y-fibrina` **no se cambia**.
**Why:** El H1/body/`Service.name` del schema ya decían "Plasma Rico en Plaquetas y Fibrina"; el resto decía "PRP y Fibrina" → Google veía dos nombres de entidad para la misma página. "PRP"/"plasma rico en plaquetas" tiene volumen de búsqueda; "PRF"/"fibrina" ≈ 0. El anchor text de links internos y el `<title>` son señales de entidad — unificarlas al H1 es la jugada. El slug se descartó: es la señal on-page más débil y cambiarlo obliga a tocar `sitemap.xml` + montar 301 en `.htaccess` (ambos archivos protegidos) + actualizar todos los `to=`; riesgo alto para ganancia marginal cuando H1 + title + anchor text ya llevan la keyword. Mobile verificado a 375px: el nombre largo entra en 1 línea en el menú, sin romper layout. **8.10** (posicionamiento de mercado) se cerró sin código: las FAQ ya diferencian PRP/PRF y explican el protocolo tópico con Dermapen; el sitio correctamente no usa "Vampire Facial®" (marca registrada); transparencia de precio es decisión de negocio.
**How to apply:** Cualquier mención nueva del tratamiento como nombre propio → "Plasma Rico en Plaquetas y Fibrina". Usar "PRP"/"PRF" solo al explicar la diferencia técnica entre las dos fracciones. Si algún día se decide migrar el slug, es un cambio grande con su propio spec: ruta nueva + 301 + `sitemap.xml` + canonical + `og:url` + todos los `to=` + (opcional) renombrar `public/assets/images/landings/prf-y-fibrina/`.

## 2026-08-27 — Hero visual test: ocultar la media, no `mask` ni `maxDiffPixelRatio`
**Decision:** En `tests/visual.spec.js` (`Home Page - Hero Viewport`) se inyecta `page.addStyleTag` con `section:first-of-type video, section:first-of-type img { visibility: hidden !important; }` antes del `toHaveScreenshot`. Baselines `home-hero-*` regenerados con la media oculta.
**Why:** El fondo del hero es un `<video>` (`HeroMedia.jsx`) cuyo frame pintado varía entre corridas → el snapshot fallaba ~1 de cada 2 veces sin regresión real. Un test siempre-en-rojo se deja de mirar y ahí se cuela un bug de verdad. Se descartó `mask: [locator video/img]` porque el `<video>` es `position:absolute; width/height:100%` cubriendo toda la sección, y Playwright enmascara el bounding box → tapaba también el H1 "SALUD PROFESIONAL PARA TU PIEL" y los CTA RESERVAR/CONTACTO. Se descartó subir `maxDiffPixelRatio` porque afloja la tolerancia de toda la pantalla (podría tragarse un botón desplazado o un cambio de color de titular). `visibility:hidden` conserva el layout, así que headline, CTAs, topbar, navbar, TrustBar y popup de WhatsApp se siguen comparando; solo desaparecen los pixeles decorativos no deterministas. Cobertura perdida ≈ nula: el video ya tiene `poster` (imagen) en `HeroMedia.jsx:25` y la rama `<img>` cae a `og-default.jpg` en `onError`, así que un fallo del video se ve igual y sería un cambio pedido explícito.
**How to apply:** Si otro snapshot se vuelve flaky por `<video>`/media no determinista, preferir ocultar el elemento con `addStyleTag` (`visibility:hidden`, conserva layout) antes que `mask` (tapa lo que esté encima) o aflojar `maxDiffPixelRatio` (global). Regenerar solo ese baseline con `-g "<nombre>" --update-snapshots` y verificar 2-3 corridas seguidas en verde.

## 2026-08-27 — Carrousel propio (`BeforeAfterCarousel`) en vez de extender `BeforeAfterGrid`; sin autoplay
**Decision:** La sección RESULTADOS de `/tratamientos-postoperatorios` usa un componente nuevo `src/components/shared/BeforeAfterCarousel/`, no una variante de `BeforeAfterGrid`. El template `LandingPage.jsx` elige entre ambos con `beforeAfter.layout === 'carousel'`. El carrousel se construye con CSS `scroll-snap` + botones (cero dependencias). No tiene autoplay. Los valores visuales del marco/chip/disclaimer se **copian** de `BeforeAfterGrid.module.css`, no se importan.
**Why:** `BeforeAfterGrid` lo comparten `LandingPage` y `TreatmentDetailPage` (varias landings + ~todas las páginas de tratamiento); meterle estado de carrousel, listeners de scroll y controles adentro sube el blast radius sobre un proyecto casi cerrado. Las 8 imágenes de postoperatorio ya traen el antes+después combinado en un solo archivo → cada slide es UNA media, no un par, así que el modelo de `BeforeAfterGrid` (2 slots + labels configurables por item) no encaja. Un componente aislado se entiende y se testea solo. Autoplay descartado por accesibilidad/movimiento y porque son fotos que la gente mira con calma (mismo criterio que el hero/team, que tampoco honran `prefers-reduced-motion` en autoplay — acá directamente no hay autoplay). No se importó el CSS module del otro componente para no acoplar dos componentes por el archivo de estilos; el costo es ~30 líneas de CSS duplicado que cambian juntas raras veces.
**How to apply:** Para poner un carrousel de resultados en otra landing: en su `beforeAfter` de `landingPages.js`, `layout:'carousel'`, `variant:'light'`, e `items: [{src, alt, type}]` con `type` ∈ `'comparison'|'result'` (`comparison` → chips ANTES+DESPUÉS, `result` → chip RESULTADO). Si algún día el marco/chip/disclaimer de `BeforeAfterGrid` cambia y se quiere paridad, actualizar también `BeforeAfterCarousel.module.css` (los valores están comentados como "mirrored from BeforeAfterGrid").

## 2026-08-27 — `FAQPage` schema: fuente única = `FAQAccordion`; las landings NO lo declaran a nivel Helmet
**Decision:** El `FAQPage` JSON-LD lo emite **solo** `src/components/shared/FAQAccordion/FAQAccordion.jsx` (vía su propio `<Helmet>`). Las 3 landings (`Postoperatorios.jsx`, `PrfYFibrina.jsx`, `LimpiezaFacial.jsx`) tenían además un `FAQPage` en el `@graph` de su Helmet de página — se eliminó. `TreatmentDetailPage` y `Contacto` ya estaban limpios (solo el de `FAQAccordion`).
**Why:** Dos `FAQPage` idénticos en la misma URL = structured data en conflicto; Google puede ignorar ambos o descartar el rich result. `FAQAccordion` se renderiza en las 3 landings (vía `LandingPage`), así que su `FAQPage` cubre el caso sin duplicar. Se eligió conservar el del componente (no el del Helmet de página) porque es el que ya está en todas las superficies con FAQ y se mantiene solo con el acordeón.
**How to apply:** Ninguna página nueva debe declarar `@type: FAQPage` en su Helmet si ya renderiza `<FAQAccordion>`. Si una página necesita FAQ schema sin acordeón visible, ese es el único caso para un `FAQPage` a nivel página.

## 2026-08-27 — `Service` schema de landings referencia `#organization` por `@id`
**Decision:** En el `Service` JSON-LD de las 3 landings, `provider` pasó de un bloque parcial `HealthAndBeautyBusiness` + `address[]` (repetido) a una referencia `{ "@type": "HealthAndBeautyBusiness", "@id": "https://dermamskinhealth.com/#organization", "name": "Derma.M" }`. Se sumó `areaServed` (City "West Palm Beach") e `image` (hero.jpg absoluto). El `name` del `Service` es un string literal en Title Case, no `hero.title` (que está en MAYÚSCULAS).
**Why:** El nodo canónico del negocio ya existe en `Home.jsx` (`#organization`, con address/telephone/geo/horarios/aggregateRating). Re-declarar un address parcial en cada landing fragmenta la entidad; referenciar por `@id` la consolida. `hero.title` en MAYÚSCULAS como `Service.name` es mala señal para Google.
**How to apply:** Cualquier `Service`/`MedicalProcedure` schema nuevo en el sitio: `provider` = `{ "@id": "https://dermamskinhealth.com/#organization" }`, nunca redeclarar el address. `name` en Title Case legible, no tomado de un campo que venga en mayúsculas.

## 2026-08-27 — `PageHero` prop `imageAlt`; `relatedLinks` en landing data
**Decision:** (a) `PageHero.jsx` acepta prop opcional `imageAlt`; usa `alt={imageAlt || title}` en el `HeroMedia`. Solo `LandingPage` la pasa (`hero.imageAlt`); el resto de callers (hubs, `TreatmentDetailPage`, `Nosotros`…) sin cambio → alt sigue siendo `title`. La imagen del bloque "problema" en `LandingPage` usa `alt={problem.imageAlt || problem.headline}`. (b) Campo opcional `relatedLinks: { label, links:[{text,to}] }` en el data de una landing → bloque `7b` en `LandingPage.jsx` (con `<Link>` de react-router) que solo renderiza si hay links. Hoy solo `postoperatorios` lo define.
**Why:** Los `alt` de hero y del bloque problema salían de `hero.title` / `problem.headline` (MAYÚSCULAS, duplicando el H1/H2) — mal para accesibilidad y SEO de imágenes. La copy de `LandingPage` se renderiza como texto plano, así que meter un `<a>` interno real (crawleable) para romper el dead-end de enlazado de la landing requería sí o sí un punto de extensión en el template; `relatedLinks` opcional es el mínimo que no afecta a las landings que no lo usan.
**How to apply:** Para dar `alt` propio al hero de una landing: `hero.imageAlt` en `landingPages.js` (redactar mirando la foto, ≤~150 char, sin caps, sin promesas). Para sumar enlaces internos a una landing: `relatedLinks` con 1–3 links de anchor text descriptivo a hubs/páginas relacionadas.

## 2026-08-27 — `TreatmentDetailPage`: sección B/A y `protocolImage` son opt-in vía `customDetails`
**Decision:** La sección "EVOLUCIÓN Y RESULTADOS ASISTIDOS" (`BeforeAfterGrid`) de `TreatmentDetailPage` solo renderiza si el slug tiene `customDetails['<slug>'].beforeAfter` con `items`. Se eliminó el fallback de convención que armaba `.../<slug>/before-after-1.jpg` / `-2.jpg`. Igual `protocolImage`: pasó de `${base}/protocol.jpg` incondicional a `custom.protocolImage || null`; el template ya usa `image` (hero) como fallback del `background-image`.
**Why:** Solo 3 de 25 páginas de tratamiento tienen antes/después reales en disco (`blanqueamiento-dental`, `limpieza-dental`, `peel-coreano`) — las 3 con override. Las otras 22 renderizaban 2 `<img>` rotos + `alt="Before"/"After"` (inglés, no descriptivo, en sitio médico en español) = 44 requests fallidos + señal de contenido thin/roto. `protocol.jpg` no existe en ninguna de las 25 carpetas → 25 requests 404 por carga que además no cambiaban nada visual (ya caía al hero). Opt-in por `customDetails` es el mismo mecanismo que ya usaban `beforeAfter` overrides; cero riesgo para las 3 páginas con contenido real, y las 22 quedan honestas (sin sección vacía).
**How to apply:** Para activar el B/A de una página de tratamiento: cargar `<slug>-antes.jpg` / `<slug>-despues.jpg` (+ webp q78) en su carpeta y agregar `customDetails['<slug>'].beforeAfter = { items:[{ before, after, beforeAlt, afterAlt }] }` (labels default ANTES/DESPUÉS). Para un `protocol.jpg` propio: `customDetails['<slug>'].protocolImage`.

## 2026-08-27 — `alt` de imágenes en `TreatmentDetailPage`: prop `imageAlt` + fallback Title Case
**Decision:** `TreatmentHero` acepta prop `imageAlt` (antes `alt=""` hardcodeado). `TreatmentDetailPage` deriva los fallbacks con un helper `titleCase()` porque los `title` de tratamiento están en MAYÚSCULAS en `categoryPages.js`: hero → `"<Tratamiento> en Derma.M, West Palm Beach"`, whatis → `"Aplicación de <Tratamiento> en Derma.M"` (antes whatis usaba `alt={title}` → `"PEEL COREANO"`). Campos opcionales `heroImageAlt` / `whatIsImageAlt` en `customDetails` para alt descriptivo por página.
**Why:** `alt=""` en la imagen LCP de las 25 páginas = señal de Google Imágenes y de a11y perdida; `alt` en MAYÚSCULAS = mala a11y (lectores de pantalla). El fallback genérico en Title Case con nombre + locale es ganancia real inmediata sin redactar 50 alts. Los campos opcionales dejan la puerta abierta al alt descriptivo por página (se llena oportunistamente, como el B/A de peel-coreano), sin bloquear el fix base.
**How to apply:** alt descriptivo por página → `customDetails['<slug>'].heroImageAlt` / `.whatIsImageAlt` (mirando la imagen, ≤~150 char, sin caps, sin promesas). Si aparece otro título de tratamiento con acrónimo, revisar que `titleCase()` lo deje aceptable en el alt (no es prosa, "Hifu" en vez de "HIFU" es tolerable).

## 2026-08-27 — `TreatmentSEO` compartido: un solo owner del <head> de páginas de tratamiento
**Decision:** Nuevo `src/components/shared/TreatmentSEO/TreatmentSEO.jsx` que renderiza todo el `<Helmet>` de éxito (title, description, canonical, robots, OG/Twitter, `@graph` con `Service` + `BreadcrumbList`). Los 5 `[treatment].jsx` (uno por categoría) lo usan en vez de repetir el `<Helmet>` inline. `titleCase()` + `clampWords()` viven en `src/utils/text.js` (primer archivo de esa carpeta). Campos opcionales `metaTitle`/`metaDescription` por tratamiento en `categoryPages.js` con fallback compuesto (`titleCase(title) en West Palm Beach | Derma.M` para el título; `description` + boilerplate recortado a 155 para la descripción). El `FAQPage` lo sigue emitiendo `FAQAccordion` — `TreatmentSEO` NO lo duplica.
**Why:** Los 5 archivos de ruta tenían el mismo `<Helmet>` copiado, difiriendo solo en el segmento de URL → cada fix del audit (title en MAYÚSCULAS, description de 41 char, sin `BreadcrumbList`, `provider` sin `@id`, sin OG) había que hacerlo 5 veces. Un componente = un cambio para las 24 páginas. Es refactor de `<head>`: cero impacto visual, `test:visual` sin diffs. Se eligió el componente compartido antes que un helper que devuelva un objeto porque `Helmet` necesita elementos JSX hijos y el patrón del proyecto ya es "componente que envuelve `<Helmet>`" (igual que los page-components de landing).
**How to apply:** Página de tratamiento nueva → su `[treatment].jsx` de categoría renderiza `<TreatmentSEO data={treatmentData} categorySlug="<segmento-url>" slug={treatment} />` y nada más de `<Helmet>` en la rama de éxito. Para afinar title/description de una página concreta: `metaTitle` / `metaDescription` en su entrada de `categoryPages.js`. `dateModified` en el schema queda pendiente para cuando haya contenido con fecha real (sub-proyecto C).

## 2026-08-27 — CWV: no se agregan width/height masivamente; tune de LCP acotado
**Decision:** No se propaga `width`/`height` a todas las imágenes del sitio "para evitar CLS". El CLS por imágenes ya está resuelto por CSS en todas las superficies: heroes (`TreatmentHero`/`PageHero`) con `min-height:70vh`/`82vh`/`calc(100vh-304px)` + media `position:absolute; inset:0`; `BeforeAfterGrid`/`BeforeAfterCarousel` con `aspect-ratio:4/5`; `LandingPage .problemImage` con `aspect-ratio:4/5` (mobile) / `height:100%` (desktop); `TreatmentCard .imageWrapper` `1/1`; `FeaturedServices` `16/9`/`4/5`; `whatIs` ya con dims. CLS medido = 0. El tune de CWV se limita a: (a) `fetchPriority="high"` + `width/height` en `HeroMedia` (imagen LCP); (b) `loading="lazy"` en las imágenes de `BeforeAfterGrid`/`BeforeAfterCarousel` (siempre bajo el fold); (c) `width/height` como pista de ratio SOLO en contenedores de ratio fijo conocido (B/A `1000×1250`, problema `800×1000`), nunca en `MediaBlock` (ratios variables → un hint fijo mentiría).
**Why:** El audit `seo-checklist-65` marcó "sin width/height" como riesgo CLS, pero es un falso positivo del análisis estático acá — cada contenedor reserva su caja por CSS. Agregar los atributos donde el `object-fit:cover` + tamaño CSS ya mandan no cambia el pixel final (lo confirma `test:visual` 22/22 sin diffs) ni mejora una métrica que ya está en 0. Lo que sí mueve LCP es priorizar la descarga del hero y no competirle ancho de banda con imágenes que están 3 pantallas abajo.
**How to apply:** Imagen nueva → confiar en el `aspect-ratio` / altura fija del contenedor para el CLS. Agregar `width/height` solo si el contenedor tiene un ratio fijo y conocido y el atributo lo respeta. `fetchPriority="high"` únicamente en la imagen LCP (hero). Todo lo que esté bajo el fold: `loading="lazy"`. Medir LCP/INP reales con PageSpeed sobre producción, no sobre el dev server.

## 2026-08-27 — C1 (respuesta directa GEO): reescritura de `whatIsBody`, no sección nueva; H2 declarativo
**Decision:** Los bloques de "respuesta directa / GEO" del sub-proyecto C se implementan **reescribiendo el `whatIsBody` existente** (sección 4 "EL PROTOCOLO" de `TreatmentDetailPage`), NO agregando una `<section>` nueva. Patrón por página: 1ª oración = definición autónoma citable (qué es + cómo + para qué), 2ª = beneficios en lenguaje compliant ("puede ayudar a mejorar…"), 3ª = señal local + "valoración médica previa". ~50–55 palabras. El H2 opcional se afina vía nuevo override `customDetails['<slug>'].whatIsHeadline` (builder: `custom.whatIsHeadline || \`TRATAMIENTO DE ${title}\``), en forma **declarativa** `Nombre: qué es y para qué sirve` — sin `¿?`. Piloto aplicado solo a `microneedling`; las otras 23 quedan con el fallback hasta escalar.
**Why:** Una sección nueva empuja 10+ bloques hacia abajo en las 24 páginas y obliga a regenerar todos los baselines de `test:visual` → blast radius alto en un proyecto near-final. La sección 4 ya ES "qué es este tratamiento" y la 3 (`TreatmentQuickFacts`) ya son los specs en bullets: solo faltaba que el texto de la 4 arranque con la definición en vez de con contexto. Reescribir un campo de datos = 0 cambio de wireframe/UI, `test:visual` 22/22 sin diffs. El H2 como pregunta con `¿?` se descartó: los headers hermanos ("CÓMO FUNCIONA…", "CUÁNDO CONSULTAR ANTES", "PARA QUIÉN ES") son etiquetas de sección sin puntuación; un `¿?` rompe ese patrón visual. La forma declarativa mantiene el keyword al inicio y los motores de IA no necesitan la puntuación literal para hacer el match de query. Expectativa comunicada al usuario: aporte marginal, valor real solo al aplicarlo a las 24 y sumado a A + local SEO.
**How to apply:** Tratamiento a "geo-izar" → reescribir `customDetails['<slug>'].whatIsBody` con el patrón de 3 oraciones (definición autónoma primero; sin palabras prohibidas de `docs/MEDICAL_COMPLIANCE.md`; sin garantías; incluir locale + valoración previa). H2 → `whatIsHeadline` declarativo con el nombre del tratamiento al inicio, coherente con el nombre público (no reintroducir entidades partidas tipo PRP/PRF). Correr `test:visual` — 0 diffs esperados salvo que ese tratamiento tenga su §3/§4 en un snapshot. C2 (dato + fuente de autoridad) es un ciclo aparte con sign-off de compliance.

## 2026-08-27 — `whatIsSection` de hidrofacial SÍ está en el visual test (corrige el spec de C)
**Decision:** El spec de sub-proyecto C asumía que la sección 4 "EL PROTOCOLO" (`whatIsSection`) no estaba en ningún baseline de Playwright. Es falso: `tests/visual.spec.js:107` ("Hidrofacial Detail Page - Viewport") snapshotea `whatIsSection`, `problemSection` y `whoForSection` **de `/faciales/hidrofacial`** en desktop-chrome y mobile-safari. Ningún otro tratamiento tiene test de secciones. Reescribir el `whatIsBody`/`whatIsHeadline` de hidrofacial (Task 2 de C) diffeó `hidrofacial-whatis-mobile-safari-win32.png` en ~3% (supera `maxDiffPixelRatio: 0.02`); en desktop-chrome el mismo cambio quedó bajo tolerancia (sección mucho más grande a 1920px). Se regeneró sólo el baseline mobile con `--update-snapshots` tras verificar en el `-diff.png` que el cambio es exclusivamente de texto (H2 + párrafo en la misma caja, sin reflow de layout). El baseline viejo además estaba desactualizado (topbar previo a `6a17f67`).
**Why:** Es un cambio de copy intencional y aprobado sobre una sección snapshoteada — el procedimiento estándar del repo (documentado en varias entradas de `PROGRESS.md`) es regenerar el baseline afectado tras confirmar que el diff es sólo lo que se buscaba. No es una regresión.
**How to apply:** Al tocar `whatIsBody`/`whatIsHeadline`/`problemContext*`/`whoForList` de **hidrofacial**, contar con regenerar `hidrofacial-{whatis,problem,whofor}-*.png` (sobre todo mobile-safari) y verificar el `-diff.png` antes de aceptar. Las tandas 3-6 de C (corporales, láser, dental, capilar) no tocan hidrofacial → no deberían generar diffs; cualquier otro baseline que diffee es alarma.

## 2026-08-27 — "medical spa", no "clínica"; marca = "Derma.M", categoría = descriptor
**Decision:** Derma.M se nombra en toda la copy nueva como un **medical spa** (2 palabras, alineado con `index.html` `<title>` y `public/llms.txt`), nunca como "clínica" (distinción regulatoria en Florida entre med spa y consultorio médico). La marca sigue siendo **"Derma.M"** a secas en nav/footer/title/schema; "medical spa" es un descriptor de categoría, NO parte del nombre — se descartó "Derma.M MedSpa" como lockup porque ninguna otra superficie del sitio lo usa y fragmentaría la señal de entidad. La señal local del `whatIsBody` de las 25 páginas de tratamiento coloca `Derma.M` + `medical spa` + `West Palm Beach` juntos (para captar "medical spa west palm beach" / "medspa wpb"), en 3 formas rotadas para no leerse como boilerplate. Se corrigieron además `landingPages.js:317`, `contactPage.js:22` y `NancyNieto.jsx` (meta ×3) que decían "clínica" para el negocio. Se dejaron intactos: `categoryPages.js:621` (testimonial verbatim de clienta), `NoticePrivacyPractices.jsx:107` (texto legal HIPAA), `aboutPage.js:167` ("práctica clínica" = experiencia profesional, uso correcto).
**Why:** Un med spa que se llama "clínica" en su copy puede implicar una categoría de práctica médica para la que no está licenciado; el usuario lo marcó como error crítico. Para SEO, tener "medical spa" + "West Palm Beach" en texto de cuerpo capta la búsqueda de categoría local sin inventar un nombre de marca nuevo. Consistencia de entidad (mismo `name` en todas las superficies) pesa más que un lockup con keyword.
**How to apply:** Copy nueva → "medical spa" (nunca "clínica"/"clinic"/"MedSpa"). Al referir al lugar físico, juntar `Derma.M` + `medical spa` + `West Palm Beach`. No editar testimonios ni texto legal para forzar el término. Si aparece "clínica" en copy de marketing nueva, es un bug.

## 2026-08-27 — Barrido de compliance en campos FAQ: reemplazar el absoluto, no el sentido
**Decision:** En los campos `faq`/`problemContext`/`whoForList` de las 25 páginas de tratamiento + `faq` de las 3 landings se eliminaron las expresiones prohibidas por `docs/MEDICAL_COMPLIANCE.md`, reemplazándolas por redacción **equivalente en sentido** pero sin el absoluto que crea exposición legal: "sin dolor"/"indoloro" → descripción de la sensación real ("suave y muy tolerable", "sin ardor ni quemaduras", "muy cómodo"); negaciones de downtime ("Cero tiempo de reposo", "Ninguno", "sin reposo posoperatorio", "al instante") → lenguaje de reincorporación ("no requiere reposo; puedes retomar tu rutina el mismo día", "de forma no invasiva"); "permanente/para siempre/definitiva" en **preguntas de resultado** → "¿Cuánto duran los resultados…?"; una cifra de eficacia sin fuente ("reducción del 85% al 90%" en depilación láser) → cualitativa ("reducción notable y duradera"). Se **conservaron** las preguntas-mito cuya respuesta arranca con un "No" honesto que desmiente el mito (estrías blancas, manchas de sol "por completo", "¿debe ser doloroso?" en drenaje postoperatorio) — es buen patrón AEO. "marcas permanentes" que describe **la condición** (no el resultado) se conserva. "sin sangrado" en microdermoabrasión / micro-infusión capilar se conserva reformulado como hecho del procedimiento ("no produce sangrado"), separado del combo "sin dolor". El campo `disclaimer` NO se toca: "Requiere valoración previa para garantizar tu seguridad y resultados" es la CTA pre-tratamiento **exigida literalmente** por `MEDICAL_COMPLIANCE.md`.
**Why:** "sin dolor" / "sin tiempo de recuperación" son frases de exposición legal independientemente de si el tratamiento es suave: basta un cliente con molestia para que el absoluto sea un problema. Reformular preservando el mensaje ("muy cómodo", "reincorporación el mismo día") mantiene la percepción del tratamiento sin el claim. Una cifra de eficacia concreta es un claim de estructura/función que exige sustanciación FTC — sin la fuente, se saca (si la clínica aporta la del fabricante, vuelve con cita en C2).
**How to apply:** Copy nueva de FAQ/beneficios → nunca "sin dolor"/"indoloro"/"sin tiempo de recuperación"/"permanente"(resultado)/"garantiza resultados"/cifra de eficacia sin fuente. Sí: "muy tolerable", "molestia mínima", "reincorporación el mismo día", "reducción notable y duradera", rangos con "según valoración". Preguntas-mito + respuesta honesta que empieza con "No" son aceptables y buenas para AEO.

## 2026-08-27 — Al cambiar el `title` de `WarningBox` se cambió también el `eyebrow`
**Decision:** El cambio pedido era solo el `title` por defecto de `WarningBox` (`'CUÁNDO CONSULTAR ANTES'` → `'PRECAUCIONES Y CONTRAINDICACIONES'`). Se cambió además el `eyebrow` por defecto (`'PRECAUCIONES DE SEGURIDAD'` → `'ANTES DE RESERVAR'`).
**Why:** El `eyebrow` se renderiza inmediatamente encima del `title` (apilado en mobile <1024px con un divider entre medio; en columnas 25%/70% en desktop). Dejar "PRECAUCIONES DE SEGURIDAD" sobre "PRECAUCIONES Y CONTRAINDICACIONES" repetía "PRECAUCIONES" en dos líneas contiguas. `'ANTES DE RESERVAR'` elimina el choque y reancla la intención de "consultar antes" (que sale del título) al CTA de reserva de la página. "Precauciones"/"contraindicaciones" no están en la lista de términos prohibidos de `docs/MEDICAL_COMPLIANCE.md`.
**How to apply:** Si alguna página de tratamiento necesita otro `title`/`eyebrow` en su `WarningBox`, pasar las props explícitamente desde `TreatmentDetailPage.jsx` (`<WarningBox title=... eyebrow=... />`) en vez de volver a tocar el default del componente. `warningSection` no está snapshoteada — un cambio de copy ahí no dispara diffs de `test:visual`.

## 2026-08-27 — Cluster de schema `#organization`: fuente única, `HealthAndBeautyBusiness`, sin `aggregateRating`
**Decision (a) — `@type` sigue `HealthAndBeautyBusiness`, NO `MedicalBusiness`/`MedicalClinic`.** Se borró `medicalSpecialty: "Dermatology"`.
**Why:** Coherente con el posicionamiento canónico "medical spa, NO clínica" (`llms.txt`, `index.html`). `MedicalBusiness` dispara escrutinio YMYL de Google y su versión honesta exige un nodo `Physician` con el director médico nombrado y credenciales — que choca con la instrucción explícita de Nancy de no publicar números de licencia (ver 2026-08-20). Además `medicalSpecialty: "Dermatology"` era un overclaim: Derma.M hace estética de la piel, no ejerce dermatología. `additionalType` a un concepto Wikidata "Medical spa" se dejó fuera por no poder verificar el Q-id con certeza.
**How to apply:** Revisitar `MedicalBusiness` solo si se crea una página pública de bio del director médico (como la de Nancy), en su propio ciclo, con nodo `Physician`.

**Decision (b) — se quitó `aggregateRating` 4.9/117 en vez de mostrar reseñas ahora.**
**Why:** La política de Review snippets de Google exige valoraciones genuinas y visibles en la misma página. `aggregateRating` hardcodeado sin reseñas on-page = riesgo de acción manual sobre todo el dominio + choca con `MEDICAL_COMPLIANCE.md`. Mostrarlas bien = pull real de Google Maps vía APIFY + componente `ReviewsSection` + diseño + test visual = sub-proyecto propio (backlog 8.20), que además depende de la sesión B (verificar el GBP y su corpus real de reseñas). `grep` confirmó que ningún componente lee `aggregateRating` — quitarlo es riesgo cero. Nota: en 2026-08-20 el usuario había confirmado que 4.9/117 eran reales, pero la fuente no está en el repo y el GBP no aparece en búsqueda (8.19) — hasta poder mostrarlas, el schema no las declara.

**Decision (c) — variantes "med spa"/"medspa" solo en `alternateName`/`keywords`, nunca en `name`.**
**Why:** `name` sigue `"Derma.M"` (la decisión de 2026-08-27 de no fragmentar la entidad con un lockup "MedSpa" sigue vigente). `alternateName` (`"Derma.M Med Spa"`, `"DermaM"`, …) y `keywords` son el lugar correcto para variantes de búsqueda sin que se conviertan en "el nombre".

**Decision (d) — `#organization` extraído a `src/data/organizationSchema.js` como fuente única.**
**Why:** El nodo estaba triplicado e inconsistente: completo en `Home.jsx`, copia parcial (sin `@id`/`url`/`logo`/`sameAs`) en `Contacto.jsx` re-declarando address/geo/hours, y otra copia parcial en `NancyNieto.jsx` (`Person.worksFor`). Eso es exactamente la deriva NAP que el audit de SEO local puntúa, + 2 entidades-fantasma. Ahora `Contacto` y `NancyNieto` referencian `#organization` por `@id`. Refactor en alcance porque el ciclo ya editaba los 3 archivos.
**How to apply:** Cualquier cambio de NAP/entidad va **solo** a `src/data/organizationSchema.js`. Las landings (`LimpiezaFacial`/`Postoperatorios`/`PrfYFibrina`) y `TreatmentSEO.jsx` todavía referencian `#organization` por `@id` inline — migrarlas a importar `organizationNode` es candidato futuro, no urgente (ya usan `@id`, no re-declaran datos).

**Decision (e) — mapa de `/contacto`: embed keyless por Place ID, NO Locator Plus.**
**Why:** El usuario bajó un export de "Locator Plus" (Quick Builder) de Google Maps Platform. Requiere una API key con cuenta de billing de Google Cloud (tarjeta obligatoria) + un `<script>` externo de `ajax.googleapis.com`. Innecesario para una sola ubicación. El `<iframe>` de Maps Embed (`?pb=` o `?q=place_id:…&output=embed`) es gratis, sin key, sin límite. El `pb=` actual apunta a un feature ID/coords erróneos (~1 km al sur). Fix = `?q=place_id:ChIJ85kuJaTX2IgRXPrdsU0jNRs&output=embed`. **Task diferida** por el usuario ("vemos la mejor estrategia después") — el Place ID quedó en el spec/plan.

## 2026-08-28 — Verificación de Google Search Console por archivo HTML en `public/`, no por `<meta>` tag
**Decision:** La verificación de propiedad de GSC para `https://dermamskinhealth.com/` se hace con `public/google2f0ede1a410e8a22.html` (método "HTML file" de GSC), no con el `<meta name="google-site-verification">` en `index.html`.
**Why:** Los dos métodos son igual de válidos y ambos deben persistir para siempre. Se eligió el archivo porque: (a) el usuario ya lo había descargado de GSC; (b) es el método que GSC marca como recomendado; (c) **no toca `index.html`** — en un proyecto near-final, menor blast radius. Vite copia `public/` verbatim al build, así que se sirve en la raíz del dominio sin config extra (verificado: `vite build` emite `dist/google2f0ede1a410e8a22.html`; dev server lo sirve con 200).
**How to apply:** No borrar `public/google2f0ede1a410e8a22.html` (GSC re-chequea). La verificación real ("Verify" en GSC) se hace **después del deploy** del sitio nuevo. El token del método `<meta>` tag (`M8yHH7yF8s5wO1AOTLzruE3HwjMiGW4hxeBgLlM9oIk`) quedó guardado en `docs/seo-setrategies/INTAKE.md` (Tema 5) por si alguna vez se cambia de método. Ídem Bing Webmaster cuando se haga: su propio archivo/tag, mismo criterio.

## 2026-08-28 — Intención local en el H1: sub-línea dentro del `<h1>`, no reescribir el titular
**Decision:** Para meter `Medical Spa` + `West Palm Beach` en el `<h1>` de Home (y, en Fase 2, de los 6 hubs) se agrega una **sub-línea `<span>` en `display:block` DENTRO del mismo `<h1>`**, con estilo de kicker (~15–16px vs. `clamp(2.5rem,5vw+1rem,4rem)` del titular; gris `#CCC9C1`; `uppercase` heredado; `tracking 0.12em`; `font-weight 400`). Texto exacto: **`Medical Spa · West Palm Beach`** (separador U+00B7 `·`, sin "en", "Medical Spa" con mayúscula inicial en el fuente). El titular editorial grande ("Salud profesional para tu piel", y los headlines de los hubs) **no se toca**. En hubs es una prop opcional `localTag` en `PageHero` que solo pasa `CategoryPage` (los otros 3 consumidores — `AboutPage`, `FounderBioPage`, `LandingPage` — no la pasan → sin cambio).
**Why:** Descartado (a) reescribir el titular grande con la ciudad integrada — la dueña ya aprobó la página y el riesgo de "queda horrible" / romper layout mobile era alto; (b) poner el locale solo en el eyebrow — es un `<p>`, no heading, no cierra la desconexión `<title>`↔`<h1>` que es el punto de #5. La sub-línea dentro del `<h1>` mete la keyword literal en el heading manteniendo la jerarquía visual (el ojo va primero al titular). `·` en vez de "en" porque lee como etiqueta de kicker, no como frase; SEO idéntico (lo que cuenta es "Medical Spa" adyacente a "West Palm Beach"). "medical spa" 2 palabras = término canónico (coherente con `index.html`, `llms.txt`, y la decisión de 2026-08-27).
**How to apply:** Nueva sub-línea local en un hero → `<span>` dentro del `<h1>`, mismo texto y estilo. En páginas que usan `PageHero`: pasar `localTag`. No reescribir titulares aprobados para forzar keywords. Fase 2 (hubs + `<title>` de Home) documentada en `docs/superpowers/plans/2026-08-28-h1-intencion-local-home-hubs.md`.

## 2026-08-28 — Quitado el eyebrow del hero de Home (solo Home)
**Decision:** Se eliminó el bloque eyebrow (rayita `<div>` + `<p>` "Centro de estética, belleza y salud") del hero de Home en `Hero.jsx`. Los 6 hubs (`PageHero`) **conservan** su eyebrow.
**Why:** En 375px el tagline partía en dos líneas ("...BELLEZA Y / SALUD") y con la rayita sumaba ruido visual arriba del titular. Aporte SEO nulo: `<p>` genérico, sin keyword ni locale, y "centro de estética" es un tercer encuadre de categoría distinto al canónico "medical spa" → quitarlo incluso limpia la señal de entidad. El H1 (con su nueva sub-línea local) pasa a liderar directo, que es lo correcto. En los hubs el eyebrow es la **categoría** ("TRATAMIENTOS FACIALES", etc.) — sí tiene valor y no se toca.
**How to apply:** Si se pide unificar, evaluar el eyebrow de los hubs en su propio ciclo (su texto es keyword, no relleno — no es el mismo caso). No re-agregar un eyebrow genérico al hero de Home.

## 2026-08-28 — GA4: snippet gtag.js en `index.html`, sin código de router para page_view de SPA
**Decision:** El tag de GA4 (`G-9272VHFT03`, property "Derma.M") se instala como el snippet `gtag.js` estándar en `<head>` de `index.html`, y NO se agrega ningún `useEffect`/listener en el router de React Router para emitir `page_view` en cambio de ruta.
**Why:** GA4 Enhanced Measurement incluye "page changes based on browser history events" activado por default — captura los `history.pushState` de React Router y emite `page_view` en cada navegación de la SPA sin código propio. Verificado en dev: al cargar `/` se envía `page_view` a `google-analytics.com/g/collect` con `tid=G-9272VHFT03` y también dispara el evento `scroll` de enhanced measurement. Agregar código de router sería redundante y además arriesga `page_view` duplicados. Snippet-only = mínimo y correcto para un proyecto near-final.
**How to apply:** Si en el futuro se necesita trackear rutas que NO cambian la URL (modales, tabs) o parámetros custom, ahí sí se agrega un `gtag('event', 'page_view', {...})` puntual — pero la navegación normal entre páginas ya está cubierta. No desactivar "Enhanced measurement" en el data stream sin reemplazarlo por tracking manual. El deploy es Vercel sin CSP (`vercel.json`/`_headers` no existen); si se agrega CSP hay que permitir `www.googletagmanager.com` (script-src) y `www.google-analytics.com` (connect-src).

## 2026-08-28 — Fase 2 de #5: hubs vía prop `localTag` en `PageHero`; `<title>` de Home con "en"
**Decision:** La sub-línea local de los 6 hubs se implementó con una prop opcional `localTag` en `PageHero` (no markup por hub), consumida solo por `CategoryPage`; texto en `categoryPages.js` `hero.localTag`. Los otros consumidores de `PageHero` (`AboutPage`, `FounderBioPage`, `LandingPage`) no la pasan → sin cambio. El `<title>`/OG/Twitter de Home pasó a `Derma.M | Medical Spa en West Palm Beach, FL` — con separador `|` y **con "en"** (a diferencia de la sub-línea del `<h1>`, que usa `·` sin "en").
**Why:** Prop opcional en el componente compartido = un solo punto de cambio para los 6 hubs, opt-in por construcción, cero riesgo para las 3 páginas que no la usan (mismo patrón que `imageAlt` en `PageHero`). En el `<title>` sí va "en": es una frase legible para el SERP/pestaña, no una etiqueta de kicker; `|` como separador de marca es el patrón que ya usan los `<title>` de hubs y tratamientos. El término es "Medical Spa" (2 palabras, canónico) reemplazando "Tratamientos Estéticos", que era un encuadre de categoría más débil y desalineado con `index.html`/`llms.txt`/schema. Los `<title>` de los 6 hubs no se tocaron (ya llevan ciudad).
**How to apply:** Sub-línea local en una página que usa `PageHero` → pasar `localTag` (string). En `<title>` nuevos: `Nombre | descriptor en West Palm Beach, FL`, término "Medical Spa" nunca "MedSpa"/"clínica".

## 2026-08-28 — Hubs: componente compartido `CategorySEO`, `ItemList` derivado, `ivTherapy` sin lista
**Decision:** El `<head>` de los 6 hubs pasa a un componente compartido `src/components/shared/CategorySEO/CategorySEO.jsx` (espejo de `TreatmentSEO`), que emite `@graph` = `CollectionPage` + `BreadcrumbList`. (a) El `ItemList` (en `CollectionPage.mainEntity`) se deriva **solo** de `data.featuredTreatments.treatments`; `complementaryTreatments` se ignora. (b) `ivTherapy` no emite `ItemList` (no tiene featured). (c) El `name` de cada `ListItem` = `t.listName || minorWords(titleCase(t.title))`, con `minorWords()` como helper **local** de `CategorySEO`. (d) `metaTitle`/`metaDescription` de cada hub se mueven a `categoryPages.js` verbatim. (e) `BreadcrumbList` sale del campo `data.breadcrumb` que ya existía sin usarse.
**Why:**
- **Componente compartido y no edición inline ×6:** mismo razonamiento que `TreatmentSEO` (2026-08-27) — 6 `<Helmet>` duplicados = 6 lugares para cada fix y drift garantizado. Ya había drift real: la hand-list de `corporales` listaba "Maderoterapia Corporal" (card cuyo CTA va a `/contacto`, no es página destacada) y omitía "Tratamientos Postoperatorios" (card destacada nº1 → landing). Derivar de `featuredTreatments` hace que el schema matchee siempre la grilla visible (requisito de Google).
- **`complementaryTreatments` fuera:** sus `ctaTo` son todos `/contacto` — no son URLs de ítem. Meterlos como `ListItem` es exactamente el bug que tenía `ivTherapy` (11 `ListItem` → misma URL = `ItemList` inválido, Google lo descarta y engaña a los crawlers de IA).
- **`ivTherapy` sin `ItemList`:** los 11 blends de IV no tienen página propia; siguen visibles como contenido del hub, así que no se pierde nada para el usuario ni para el crawler (lee el texto). Se descartó un `ItemList` con `ListItem` sin `url` (solo `name`): no está claro que Google lo prefiera a no tener lista y suma complejidad (YAGNI).
- **`minorWords()` local, no tocar `titleCase` compartido:** `titleCase('PLASMA RICO EN PLAQUETAS Y FIBRINA')` → `'Plasma Rico En Plaquetas Y Fibrina'`, que rompe el nombre público canónico ("en"/"y" en minúscula — ver 2026-08-27) y el `name` de schema es señal de entidad. Cambiar `titleCase` afectaría las 25 páginas de tratamiento vía `TreatmentSEO` (blast radius alto en proyecto near-final). El helper local baja a minúscula `de del la el los las y e o u en con para por a al un una` cuando no son la 1ª palabra. `listName` (campo opcional en la entrada de `featuredTreatments.treatments`) cubre lo que ni eso resuelve — hoy solo `IPL` (`titleCase` → `'Ipl'`). `HIFU` → `Hifu` se deja (aceptable en contexto no-prosa, ya documentado en `text.js`).
- **`metaTitle`/`metaDescription` a la data:** mismo patrón que ya usan los tratamientos en `categoryPages.js`. Fuente única, y `CategorySEO` los reproduce verbatim → `<title>`/description de los 6 hubs no cambian ni un carácter.
**How to apply:** Hub nuevo → su `hubs/*.jsx` renderiza `<CategorySEO data={categoryPages.<key>} />` y nada de `<Helmet>` inline; agregar `metaTitle`/`metaDescription`/`breadcrumb` al objeto en `categoryPages.js`. Para que un tratamiento aparezca en el `ItemList` del hub tiene que estar en `featuredTreatments.treatments` con `to`/`link` real (no en `complementaryTreatments`). Si `titleCase`+`minorWords` no da bien el nombre, `listName` en esa entrada. Los `<title>` de los hubs NO llevan "medical spa" (ya tienen categoría + ciudad; agregarlo los pasa de ~60 car.). Breadcrumb visible sigue pendiente (otro ciclo).

## 2026-08-28 — Heros efectivos: sub-línea local en tratamientos+landings, eyebrow de landings, heroDescription opt-in
**Decision:** (A) La sub-línea `Medical Spa · West Palm Beach` dentro del `<h1>` se extiende a las 25 páginas de tratamiento (`TreatmentHero` prop `localTag` + `.localTag` CSS) y las 3 landings (`LandingPage` la pasa a `PageHero`, que ya la soportaba). (B) El `eyebrow` de las 3 landings pasa de `TRATAMIENTO DESTACADO` (idéntico ×3) al eyebrow de su hub padre verbatim (`TRATAMIENTOS FACIALES` / `TRATAMIENTOS CORPORALES`). (C) El sub-copy del hero de las 25 páginas de tratamiento se alimenta de un campo nuevo opt-in `customDetails['<slug>'].heroDescription` (builder: `custom.heroDescription || description`), no de `description`.
**Why:**
- **(A)** Tras Fase 1 (Home) y Fase 2 (6 hubs), las páginas de tratamiento y landings — las de mayor intención de búsqueda ("[tratamiento] west palm beach") — tenían el `<title>` con ciudad pero el `<h1>` sin ninguna señal de entidad ni locale. La asimetría la marcó el usuario. Mismo patrón probado, sin reescribir el `<h1>` (nombre del tratamiento). Ahora el patrón entidad+locale-en-el-heading cubre todo el sitio.
- **(B)** El eyebrow es texto visible pegado al `<h1>`; `TRATAMIENTO DESTACADO` no orienta, no es keyword y es idéntico en las 3. El `<h1>` de la landing es el nombre del tratamiento → lo que falta cerca del heading es la **keyword de categoría**. Usar el eyebrow del hub padre: (1) es consistente con el sistema (hubs y páginas de tratamiento ya usan la categoría como eyebrow/badge), (2) ata la landing a su hub, (3) cero riesgo de compliance. Descartado: términos descriptivos ("BIOESTIMULACIÓN FACIAL" = jerga de industria, volumen de búsqueda ~0); quitar el eyebrow (mueve el layout de `PageHero` — divider + eyebrow — más que reemplazarlo). Es un ajuste chico, no palanca de ranking.
- **(C)** El sub-copy del hero era un fragmento telegráfico sin verbo ("Remodelación de textura y poros.") — el copy más débil del sitio, en las páginas que facturan. Pero el mismo `description` corto se usa en `specs['Objetivo principal']` (celda de tabla), en la interpolación de `whatIs.body` (`${description.toLowerCase()}`) y en las cards de "Te puede interesar" — expandirlo ahí los rompería. Por eso `heroDescription` es un campo separado y opt-in (fallback a `description`), mismo patrón que los otros overrides de `customDetails` (`whatIsBody`, `heroImageAlt`, etc.). Las 25 oraciones (~12–22 palabras) pasan `docs/MEDICAL_COMPLIANCE.md` — sin "sin dolor/permanente/sin downtime/garantiza", sin cifras, sin cura ("atenuar la apariencia" / "mejorar el aspecto", no "elimina"/"cura"). `heroDescription` NO repite "valoración previa": el `disclaimer` ya se renderiza como línea aparte en el hero de tratamiento.
**How to apply:** Sub-línea local en un hero nuevo → `<span>` dentro del `<h1>`, texto `Medical Spa · West Palm Beach`; si usa `PageHero` o `TreatmentHero`, pasar `localTag`. El string queda inline en 4 lugares (`Hero.jsx`, `categoryPages.js` ×6, `TreatmentDetailPage.jsx`, `LandingPage.jsx`) — no se creó constante compartida (consistente con cómo ya estaba). Para dar sub-copy de hero propio a un tratamiento → `customDetails['<slug>'].heroDescription` (una oración, compliant, sin repetir el disclaimer). Eyebrow de una landing nueva = eyebrow del hub al que pertenece.

## 2026-08-28 — `heroDescription`: derivarlo del `whatIsBody`, no redactarlo de cero (supersede parte (C) de la entrada anterior)
**Decision:** Los 25 `heroDescription` (sub-copy del hero de tratamiento) se derivan comprimiendo la 1ª/2ª oración del `whatIsBody` de cada página, reordenada **beneficio-primero**. NO se redactan como texto nuevo. Reemplaza el enfoque del commit `d247b54` (síntesis propia).
**Why:**
- **No inventar.** El primer intento metió datos que no están en la fuente vetada (p. ej. "exfoliación" en el hidrofacial, "microagujas finas" en microneedling) — plausibles pero no son el protocolo de Derma.M. El `whatIsBody` de C1 se escribió con conocimiento real del protocolo y ya pasó compliance; comprimirlo mantiene cada hero anclado y coherente con la sección 4 de su propia página.
- **CRO.** El `whatIsBody` arranca mecanismo-primero (se diseñó como definición citable para motores de IA). Para el hero eso es débil: el `<h1>` ya es la feature (nombre del tratamiento), el sub-copy es el lugar del "y a mí qué" → tiene que liderar con el beneficio. La cláusula de resultado suele estar en la **2ª oración** del `whatIsBody` ("Puede ayudar a mejorar…"); se pone primero y el mecanismo de la 1ª oración va comprimido detrás.
- **Caveats fuera de la cabecera.** Cuando la 2ª oración del `whatIsBody` es una limitación honesta ("sin eliminarlas por completo", "no recupera folículos", "no reemplaza tratamientos periodontales"), NO se lidera el hero con eso — el beneficio sale de la cláusula de propósito de la 1ª oración; el caveat sigue vivo en `whatIsBody`/FAQ.
**How to apply:** `heroDescription` nuevo o editado → tomar la 1ª+2ª oración del `whatIsBody` de esa página, no inventar mecanismo ni beneficio. Estructura: [resultado que le importa al visitante] + [mecanismo comprimido] + [reaseguro opcional]. Una oración, ~16–26 palabras, sin palabras prohibidas de `MEDICAL_COMPLIANCE.md`, sin caveat en la cabecera. Si `whatIsBody` cambia, revisar el `heroDescription` de esa página.

## 2026-08-28 — Inyectables (Botox/neuromoduladores): se ofrecen, figuran en el GBP, NO en el sitio (corrige "does not offer")
**Decision:** El médico de Derma.M administra Botox / neuromoduladores como servicio real. Figura como **ítem de lista en la descripción del Google Business Profile** (no como titular). El sitio web — landings, páginas de tratamiento, `llms.txt`, schema — **sigue sin contenido de inyectables**. Se corrigió `docs/MEDICAL_COMPLIANCE.md` línea 20 (decía "injectables (Botox, fillers, Kybella) Derma.M does not offer"). La entrada de `docs/DECISIONS_ARCHIVE.md` 2026-08-20 sobre elegir el doc de compliance queda como historia; esta la supersede en cuanto al hecho "no ofrece Botox".
**Why:** El usuario confirmó (2026-08-28) tras hablar con el médico. La descripción del GBP que redactó Nancy ya nombraba "Botox" → sacarlo hubiera sido el cambio, no dejarlo. Todos los competidores del análisis (Elite, Dr.Hamadiya, JECT, Renew You, Pure Skin) lo listan. No se lleva al sitio porque el posicionamiento de marca de Derma.M es "medical spa con criterio, sin empujar inyectables" — figurar como opción para quien lo busca ≠ construir la marca alrededor. **Sin atribución de quién lo administra en la copy del GBP** — la delegación médica es tema operativo, no de descripción (el usuario lo pidió explícitamente: "esto no pondrá a cargo del médico").
**How to apply:** "Botox" va como ítem plano en la lista de servicios de la descripción/Services del GBP, sin frases tipo "a cargo del médico" / "administrado por el médico". NO agregarlo a `src/data/*`, `public/llms.txt`, ni schema del sitio sin una decisión nueva. Si Derma.M corre **ads pagados** (Meta/Google) nombrando Botox → exige certificación LegitScript (`MEDICAL_COMPLIANCE.md` línea 29) antes de publicar.

## 2026-08-28 — Apify OK para scrapes one-off baratos (matiza "sin herramienta paga")
**Decision:** Se usó Apify (`compass/crawler-google-places`, `apify/website-content-crawler`, `apify/google-search-scraper`) para el research de competencia/servicios. Gasto ~$5-6 one-off. Matiza la línea de `INTAKE §3` / guía de Pedro ("sin herramienta paga; browser + skills"): **sin suscripción recurrente (Semrush/DataForSEO) sigue firme; scrape one-off barato con aprobación del usuario está OK.**
**Why:** El browser manual para 40+ queries × local pack + 14 perfiles + 14 sitios + 130 reseñas sería horas y menos completo/reproducible. Free tier de Apify ($5/mes) casi lo cubre. Token en `.env.local` raíz (`APIFY_API_KEY`, formato `KEY: valor`).
**How to apply:** Para research puntual que el browser haría lento/incompleto, Apify vía API está permitido con OK del usuario y estimación de costo previa. NO suscripciones mensuales. Correr por-query (el actor deduplica lugares a nivel run → un solo run con N queries no da frecuencia cross-query). Free tier ~2 runs concurrentes → serializar o `memory=1024`.

## 2026-08-28 — GBP Services: portafolio completo, tiereado por ROI + dolor cultural (no flat)
**Decision:** Cargar los ~25 tratamientos del sitio + IV (=1) como GBP Services, pero **tiereados**, no plano. Tier 1 (posicionamiento pesado, calibrado contra competencia): limpieza facial profunda, tratamientos postoperatorios/drenaje linfático, Plasma Rico en Plaquetas y Fibrina (PRF). Tier 2 (GBP Service fuerte + FAQ): hidrofacial, acné, manchas/cicatrices, rejuvenecimiento, microneedling, depilación láser, IPL, capilar. Tier 3 (cohesión, batch liviano): resto de faciales, 8 corporales, 2 dentales, IV. Descripciones derivadas del `whatIsBody`/`description` de cada tratamiento (no inventar), ≤300c, compliance-check.
**Why:** Cargar plano diluye el foco de los 3 must-win. El tiering: (1) cohesión sitio↔GBP es factor de relevancia (Pedro) y hoy el GBP es una sombra flaca del sitio — DERMA.M invisible en 37/40 queries del research; (2) servicios/fotos/posts se pueden editar sin problema (a diferencia de nombre/categorías); (3) validado empíricamente por las 130 reseñas: faciales 22 menciones, corporal/postop 13+10, PRF solo 3 (apuesta forward), capilar/IV 0. Eje cultural (demografía latina South FL): postop = dolor agudo (mucha cirugía, pocos proveedores, cirujanos no lo recomiendan → DERMA.M educa/crea demanda); manchas/melasma alta carga emocional en piel latina; "limpieza facial" es ritual cultural → usar término ES, no "deep cleansing facial".
**How to apply:** Servicio nuevo de GBP → derivar descripción del `whatIsBody` de ese tratamiento, ≤300c, sin inventar mecanismo/beneficio, compliance vs `MEDICAL_COMPLIANCE.md`. Categorías: solo agregar `Skin care clinic` ahora (4/4 líderes la tienen); resto de secundarias → una sola pasada con Tema 7 (GMBspy). Naming PRF canónico: **"Plasma Rico en Plaquetas y Fibrina (PRF)"** literal en landing/schema/llms.txt/ads/GBP; matar `PRPF` (Google no lo parsea); NO targetear `PRP` pelado (mercado ortopédico). Detalle en `docs/seo-setrategies/COMPETENCIA-SERVICIOS-2026.md`.

## 2026-08-28 — Diferenciador de DERMA.M = relacional, no resultados (evidencia: 130 reseñas)
**Decision:** El copy y posicionamiento del sitio y del GBP deben liderar con el trato relacional (te escuchan, te explican, te acompañan, en español, lugar seguro/limpio), no con "mejores resultados".
**Why:** Cross-análisis de las 130 reseñas de DERMA.M (2026-08-28): "atención/trato/escuchan/acompañan" 53 menciones (41%), "confianza/seguridad/higiene" 46 (35%), "explican/asesoran" 24 (18%), "resultados/cambios visibles" solo 22 (17%). Nancy nombrada en 20 (15%). Coincide con el dolor cultural de la demografía (latinas South FL: sentirse no escuchadas en med spas English-only). Es el diferencial *probado por los propios clientes*.
**How to apply:** GBP description, tono de FAQ, plantillas de respuesta a reseñas, framing de los 3 servicios Tier 1 → "te escuchamos, te explicamos, te acompañamos". Featurear a Nancy (refuerza E-E-A-T bio). `/nosotros` debe reflejar esta coherencia de trato explícitamente. No liderar con lenguaje de resultado/tecnología.

## 2026-08-28 — Mapa de redirects viejo→nuevo: validado contra el `.htaccess` existente
**Decision:** El `.htaccess` ya tenía el trabajo de redirects hecho pero con 2 fallas: (1) el bloque de 301s legacy estaba **debajo** del fallback SPA `RewriteRule . /index.html [L]` → no se ejecutaba ninguno; (2) 27 URLs vivas del sitio viejo (23 páginas de tratamiento + 4 posts lorem ipsum) sin ninguna regla. Se reordenó (HTTPS → non-www → 301s legacy → strip trailing slash → SPA fallback) y se completó el mapa. `.htaccess` NO se tocó — el archivo completo listo para aplicar quedó en `docs/seo-setrategies/REDIRECT-MAP-VALIDATION-2026.md` §8, se aplica el día del deploy (archivo protegido, paso a paso). Host confirmado por el usuario: **Hostinger/Apache** (no Vercel — `INTAKE.md:56` quedó stale).
**Why:**
- Inventario autoritativo = `wp-sitemap.xml` (58 URLs), no el scrape ni el `site:` de Google (subset). El WP sirve 200 en páginas inexistentes (soft-404) → se validó por `<title>` real, no por status.
- Orden de reglas: con el catch-all SPA arriba, `[L]` + rewrite a `/index.html` mata todo 301 posterior → Googlebot en URLs viejas recibía el shell sin ruta y sin señal 301 (link equity perdido).
- Decisiones de mapeo: se mapea el **slug** (lo que tiene links/indexación histórica), no el body viejo. `/plasma-rico-en-plaquetas/` (+`-2`) → `/prf-y-fibrina` (consolida el término must-win) aunque el body viejo sea "Limpieza Facial Profunda". `/dermoabrasion-2/` → `/corporales/maderoterapia-corporal` (excepción: mapeado por contenido, el slug miente). Sin equivalente 1:1 → hub más cercano, nunca 404 (`terapia-luz-led`, `mesoterapia`, `bb-glow`, `bb-lips`, `dermaplaning`, `cavitacion`, `vacuumterapia`, `desintoxicacion-ionica`, `camuflaje-capilar`). `/author/nancy-marcela/` → `/nosotros/nancy-nieto` (antes genérico → `/`). `portfolio-item/*` genérico → `/faciales` (los 8 son de tema facial).
**How to apply:** Deploy → reemplazar `public/.htaccess` con el bloque de §8 del doc, luego verificar cada fila con `curl -I https://dermamskinhealth.com/<old-path>` (un solo 301 directo al destino, sin segundo salto). URL vieja nueva a mapear → 1:1 si hay página nueva del mismo tema; si no, al hub; por contenido solo si el slug miente. Nunca dejar caer en el fallback SPA.

## 2026-08-28 — Track B GBP: rescope a "llenar gaps sin tocar lo de Nancy" + descripción validada contra guía Google
**Decision:** El plan original de Track B ("cargar 3 GBP Services Tier 1") se descartó al abrir Edit services: el GBP ya tenía ~80 servicios en 5 categorías, cargados por Nancy (incluidos varios fuera del sitio: Ultherapy, exosomas faciales, PDRN/polinucleótidos, PDRN esperma de salmón). Nuevo alcance: **cruzar los ~25 tratamientos del sitio + 3 landings + 6 hubs contra la lista GBP existente y agregar SOLO los gaps reales; cero deletes, cero renames de entradas de Nancy.** Se agregaron 12: microneedling, tratamiento capilar, terapia IV, plasma frío facial, carboxiterapia facial, radiofrecuencia facial, carboxiterapia corporal, levantamiento de glúteos sin cirugía, marcación abdominal no invasiva, HIFU corporal, tratamiento de estrías y celulitis, lipo 360 estético. Cada uno con descripción ≤300c derivada del `whatIsBody` de esa página (no inventar), compliance-check. Categoría secundaria `Skin care clinic` agregada (las 5 previas intactas). Descripción del profile reemplazada por la versión de 690c.
**Why:**
- **No borrar lo de Nancy:** decisión explícita del usuario ("eso lo agregó Nancy, que la gente lo busque"). El objetivo real es que lo del **sitio** tenga presencia en el GBP, no limpiar el GBP. Los servicios de compliance dudoso (salmón/exosomas/PDRN) quedan como están; auditar/reescribir eso sería otro ciclo con sign-off de la clínica.
- **Dental fuera:** blanqueamiento y limpieza dental no tienen dónde ir (no hay categoría dental en el GBP). Agregarlos exige categoría nueva → se difiere a Tema 7 (Pedro: no churnear categorías sin la pasada de GMBspy).
- **Descripción validada contra `support.google.com/business/answer/3038177`:** el borrador de §S3 (`COMPETENCIA-SERVICIOS-2026.md`) cumplía ~95%. Faltaba la antigüedad (Google la lista como recomendada; el perfil ya tiene "Opening date: 1 Aug 2021" → se agregó "dedicado desde 2021"). "Agenda tu valoración por WhatsApp" nombraba un canal de contacto (Google desalienta datos de contacto en la descripción; el ejemplo oficial permite CTA suave tipo "Come visit us today!") → cambiado a "Agenda tu cita de valoración." Todo lo demás OK: ≤750c (690), sin URLs/HTML/tel, sin promo/precios, `DERMA.M`/`IPL`/`PRF`/`IV` no violan la regla de mayúsculas (nombre de marca / siglas estándar), naming PRF canónico "Plasma Rico en Plaquetas y Fibrina (PRF)".
- **UI del GBP en el browser pane es inestable:** los diálogos se cuelgan/cierran solos y la lista hace reflow entre screenshot y click. Método que funcionó: reload de la página → abrir Edit services → scroll en pasos chicos → screenshot → verificar el nombre en el campo "Service" del editor ANTES de escribir descripción → guardar. Dos veces se abrió por error el editor de un servicio de Nancy (Estimulación muscular EMS, Radiofrecuencia corporal) → Cancel sin tocar. Para la caja de descripción, `key` (ctrl+a, backspace) NO funciona en ese textarea; solo `type` y selección con mouse (shift+click / triple_click). Al final lo pegó el usuario a mano.
**How to apply:** Servicio GBP nuevo → derivar descripción del `whatIsBody` de ese tratamiento, ≤300c, sin inventar, compliance vs `MEDICAL_COMPLIANCE.md`, y **verificar el nombre en el editor antes de escribir** (la lista hace reflow). Nunca borrar/renombrar servicios que no cargamos nosotros. Descripción del profile: reglas duras en `support.google.com/business/answer/3038177` — sin URL/HTML/tel/enlaces, ≤750c, foco en el negocio (no promo/precio), incluir antigüedad, CTA suave sí pero sin nombrar canal de contacto. Tier 2/3 batch de servicios y dental siguen pendientes (NEXT).

## 2026-08-28 — Dental FUERA del GBP por completo (riesgo regulatorio, no solo churn de categoría)
**Decision:** Blanqueamiento dental y limpieza dental NO se cargan como GBP Services, y NO se agrega ninguna categoría dental (`Dentist`, `Dental clinic`, `Teeth whitening service`, `Cosmetic dentist`). Supersede la línea "Dental fuera → se difiere a Tema 7" de la entrada anterior y el "2 dentales" del Tier 3 en "GBP Services: portafolio completo" (2026-08-28): ya no es un tema de no-churnear categorías, es que el ítem no va.
**Why:** Decisión del usuario (2026-08-28). Listar servicios/categoría dental en el GBP de un medical spa implica scope de odontología con licencia. En Florida el teeth whitening por no-dentistas es zona gris con historial de enforcement; "limpieza dental" (profilaxis) es claramente procedimiento de profesional dental. El framing "dental estético" no despeja el riesgo. El costo de omitirlo del GBP es ~0 (0 menciones en 130 reseñas, Tier 3).
**How to apply:** No agregar servicios ni categorías dentales al GBP. Si en el futuro se quiere presencia dental, primero sign-off de compliance de la clínica sobre quién administra y bajo qué licencia. **Abierto (blast radius mayor, ciclo aparte):** el sitio hoy tiene `/dental-estetico` hub + `/dental-estetico/blanqueamiento-dental` + `/dental-estetico/limpieza-dental` en vivo. Si el riesgo aplica al GBP, revisar si aplica también a esas páginas — decisión del usuario, con brainstorm propio, NO tocar sin pedido explícito.

## 2026-08-28 — Arquitectura de contenido del founder: Home = primer, /nosotros = puente, /nancy-nieto = persona
**Decision:** La narrativa de Nancy Nieto se reparte por "beats" con una sola página dueña de cada uno, en funnel de profundidad progresiva Home → `/nosotros` → `/nosotros/nancy-nieto`. Home `FounderSection`: solo credencial (1 línea) + filosofía relacional (1 frase) + link de salida a `/nosotros` (antes: 3 párrafos hardcodeados, sin CTA, dead-end en la 2ª sección). `/nosotros` spotlight: copy-puente enfocada a la organización, mantiene "Conoce más sobre Nancy →". `/nancy-nieto`: única con la persona completa, `founderBioPage` deja de referenciar `aboutPage.founderSpotlight`. Toda la copy del founder se consolida en `src/data/aboutPage.js`. Sub-H1 `Medical Spa · West Palm Beach`: **sí** en `/nosotros` (H1 = enunciado de valor, mismo tipo que los hubs, consistencia de entidad en página de nav primaria), **no** en `/nancy-nieto` (H1 = nombre propio → desajuste semántico; la asociación entidad↔lugar la dan `Person.worksFor` + 1 mención natural en la prosa del hero).
**Why:** Las 3 superficies repetían los mismos 3-4 beats con distinta redacción → canibalización `/nosotros` vs `/nancy-nieto` (near-duplicate body, ambas apuntan a "Nancy Nieto"), thin content en la dedicada, fuga de conversión en Home (mejor scroll position gastado en bio larga sin siguiente paso), y clic sin recompensa en "Conoce más sobre Nancy →". No se borra la página dedicada: su schema `Person` + `sameAs` + `worksFor` es activo E-E-A-T. Material biográfico real lo pasó la clínica el 2026-08-28 (rico en filosofía/visión, sin cronología — fechas/instituciones/año de apertura se layerean después sin cambio estructural). La página vieja publicada `/acerca-de/` no aporta copy reutilizable (voz de salón + overclaims); solo el concepto "escuchar", ya cubierto por la sección "ESCUCHAR ANTES DE RECOMENDAR".
**How to apply:** Copy nueva del founder → `src/data/aboutPage.js`, nunca hardcodeada en componentes. Sin números de licencia (ver 2026-08-20). "tratamientos seguros"/"resultados reales" solo como filosofía de Nancy en 1ª persona / "para ella…", nunca como promesa del sitio. Ciclo 2 y 3 pendientes en NEXT; cada uno cierra con `test:visual` (ninguna de las 3 superficies está en baseline salvo `home-founder`) + cross-check `MEDICAL_COMPLIANCE.md`. Spec/plan: `docs/superpowers/{specs,plans}/2026-08-28-founder-content-architecture*`.

## 2026-08-28 — Ciclo 2 founder: reestructura del tope de /nosotros (fusión + podas)
**Decision:** El tope de `/nosotros` (todo antes del equipo) se reestructuró sin replanteo total. (1) Se fusionaron "Founder Philosophy" (`ESCUCHAR ANTES DE RECOMENDAR` + cita de Nancy) y "Approach" (`CÓMO CUIDAMOS CADA DECISIÓN` + 3 columnas VALORACIÓN/CRITERIO/ACOMPAÑAMIENTO) en una sola sección oscura — antes decían la misma tríada 2 veces y el hero una 3ª. Nueva sección: eyebrow `NUESTRO ENFOQUE`, heading `ESCUCHAR ANTES DE RECOMENDAR`, intro + `supportingText` + 3 pilares (`BenefitColumns variant="dark"`, sin `sectionHeader` propio) + cita corta de Nancy al cierre. (2) Se eliminó `approach.conversion` (bloque "TU CUIDADO COMIENZA CON UNA CONVERSACIÓN" + CTAs anidado en la lista de beneficios). (3) Se eliminó la sección `academy` de `/nosotros` entera; en su lugar 1 línea en la Founder Spotlight ("Además, Nancy forma a otras profesionales de la estética a través de DERMA.M Academy."). (4) Hero `body` reescrito para no enumerar la tríada. (5) `hero.localTag: "Medical Spa · West Palm Beach"` agregado (sub-H1) — **sí** en `/nosotros`, **no** en `/nancy-nieto`. `founderSpotlight.body`/`secondaryBody` recortados. Orden final: Hero → Founder Spotlight (+línea Academy) → Filosofía y enfoque → Equipo → Testimonios → FinalCTA.
**Why:** El usuario revisó `/nosotros` en vivo y marcó que el tope "tiene mucha mezcla de cosas que no hacen sentido" — el diagnóstico real es falta de progresión narrativa: la misma tríada re-vestida en 3 formatos (prosa del hero, sección oscura de filosofía, lista de 3 columnas), un CTA fuera de lugar dentro de una lista informativa, y Academy huérfana entre "enfoque" y "equipo". Academy además NO es formación del equipo interno ni un servicio para clientas — es un proyecto de **capacitación para profesionales externas** (la misión de Nancy de generar oportunidades para otras mujeres del rubro), así que en `/nosotros` solo aporta como señal de autoridad (1 línea) y el desarrollo va a la página de Nancy. Sub-H1 sí en `/nosotros` porque su H1 es un enunciado de valor (mismo tipo que los hubs) y suma consistencia de entidad en una página de nav primaria; no en `/nancy-nieto` porque su H1 es un nombre propio (desajuste semántico) y la asociación entidad↔lugar la dan `Person.worksFor` + prosa.
**How to apply:** `enfoque` reemplaza a `founderPhilosophy` + `approach` en `aboutPage.js`; `academy` y `approach.conversion` borrados. `bookingUrl`/`whatsappUrl` en `AboutPage.jsx` se quitaron (solo los usaba el bloque de conversión). El export `founderBioPage` quedó con `founderPhilosophy: aboutPage.enfoque` como puente temporal — el Ciclo 3 lo reescribe standalone. `BenefitColumns` soporta `variant="dark"` (`.dark` en su module.css). `SectionHeader` acepta `titleId` para el `aria-labelledby`. Spec/plan: `docs/superpowers/{specs,plans}/2026-08-28-founder-content-architecture*` (sección "Cycle 2 re-scope").

## 2026-08-28 — Ciclo 2b founder: /nosotros es la página del equipo, no una 2ª página de Nancy
**Decision:** Tras el Ciclo 2, `/nosotros` seguía con dos bloques centrados en la fundadora (Founder Spotlight + la sección "enfoque" con la cita de Nancy y framing de filosofía). Se podó: (1) Founder Spotlight queda mínima — credencial + una frase de origen + link a `/nancy-nieto`; se quitan el párrafo de visión, "Para Nancy, cuidar también significa…" y la mención de Academy. (2) La sección fusionada queda como banda corta "CÓMO TRABAJAMOS / TRES PASOS EN CADA PLAN DE CUIDADO" con solo los 3 pilares; se quitan `enfoque.body`, `enfoque.supportingText` y el `<blockquote>` de Nancy. Todo el contenido de Nancy-persona (visión, framing `ESCUCHAR ANTES DE RECOMENDAR`, cita corta, Academy) se concentra en `/nancy-nieto` (Ciclo 3). **Academy: cero mención en `/nosotros` en el interín** (el usuario eligió opción 1 sobre re-agregar una línea de autoridad).
**Why:** El usuario, viendo el Ciclo 2 en vivo: "tiene tanta información en esa sección que es un mierdero… tienes dos secciones de la fundadora… define qué vamos a llevar a donde Nancy". `/nosotros` debe presentar al equipo y cómo trabaja, no repetir la biografía/filosofía de la fundadora. La página de Nancy es el lugar único para su visión, filosofía y Academy. La cita corta se descarta a favor de la larga (near-duplicativas; no poner dos citas de la fundadora en su página).
**How to apply:** `founderBioPage.founderPhilosophy` quedó como objeto inline temporal (copy `ESCUCHAR ANTES DE RECOMENDAR`) — el Ciclo 3 reescribe `founderBioPage` como export standalone sin referencias a `aboutPage.*` y funde ese framing en la sección `filosofia`. `enfoque` en `aboutPage.js` ya no tiene `body`/`supportingText`; `founderSpotlight` ya no tiene `secondaryBody`/`academyLine`. Plan: Task 2b + Task 3 en `docs/superpowers/plans/2026-08-28-founder-content-architecture.md`.

## 2026-08-28 — Ciclo 3 founder: /nosotros/nancy-nieto standalone, cierre del plan
**Decision:** `founderBioPage` en `src/data/aboutPage.js` pasa a objeto standalone sin ninguna referencia a `aboutPage.*` (se elimina el puente inline `founderPhilosophy` de la Task 2b). Estructura propia: `hero` / `historia` (formación y trayectoria: dos países, capacitación continua) / `filosofia` (`ESCUCHAR, EDUCAR Y ACOMPAÑAR`, absorbe el framing `ESCUCHAR ANTES DE RECOMENDAR`) / `dermamYAcademy` (fundación de DERMA.M + DERMA.M Academy + visión hacia adelante: oportunidades para otras mujeres profesionales, expansión) / `quote` (solo la larga) / `cta`. `FounderBioPage.jsx` gana la sección Historia (reusa el layout foto+texto de la ex-Spotlight, foto `/assets/images/home/founder.jpg`) y la sección DERMA.M & Academy (Off-White `#EFEFEB`, sin imagen); la sección Filosofía + cita larga se mantiene sobre Dark Authority. Cierra el plan founder-content-architecture completo.
**Why:** Principio de de-duplicación del plan: una sola página dueña de cada beat. `/nosotros` (Ciclo 2b) se quedó con equipo + cómo-trabajamos; toda la persona de Nancy (formación, filosofía, visión, Academy) es de `/nancy-nieto`. La copy es verbatim del material biográfico que pasó la clínica el 2026-08-28 — rico en filosofía/visión, sin cronología (fechas/instituciones/año se pueden layerear después sin cambio estructural). Se descarta la cita corta (near-duplicativa de la larga; no dos citas de la fundadora en su propia página). Sin sub-H1 en esta página (H1 = nombre propio); la asociación entidad↔lugar la dan `Person.worksFor` en el JSON-LD + la mención "medical spa en West Palm Beach" en el hero body.
**How to apply:** Copy nueva del founder → `src/data/aboutPage.js`, nunca hardcodeada. Sin números de licencia (ver 2026-08-20) — "Flebotomista Certificada", "licencia aprobada por el Estado de Florida" en cualitativo. "tratamientos seguros, éticos y orientados a resultados reales" aparece 1× dentro de la frase de visión fundacional de Nancy, no como promesa del sitio, no se repite. Sin cambios de schema (`NancyNieto.jsx` `ld+json` intacto). `MEMORY.md` "Founder bio page" actualizado: ya no "reuses by reference". Plan: Task 3 en `docs/superpowers/plans/2026-08-28-founder-content-architecture.md`.

## 2026-08-28 — Track A #1: término EN "deep cleansing facial" sin tocar `<title>` ni copy español
**Decision:** El término target en inglés "deep cleansing facial" se incrusta en `/limpieza-facial-profunda` en 3 capas aditivas: (1) `meta description` + `og:description` + `twitter:description` (string nueva, término al frente, 135c); (2) `Service` JSON-LD `alternateName: "Deep Cleansing Facial"`; (3) un 6º ítem de FAQ **en español** (pregunta puente de equivalencia) que arrastra las intenciones extraction / oily skin / acne y entra al `FAQPage` schema. El `<title>` queda **intacto** y no se toca H1 ni above-the-fold. `public/llms.txt` (archivo protegido): solo la línea 38, reorden de label `Deep Facial Cleansing` → `Deep Cleansing Facial`, URL sin cambios — excepción puntual aprobada por el usuario; la propagación PRF en `llms.txt` sigue siendo Track A #3.
**Why:** Research Apify (`COMPETENCIA-SERVICIOS-2026.md` §S5.4 + Stage 3): "deep cleansing facial" es búsqueda de consumidor real y ningún competidor de WPB titula una página con el término verbatim → hueco. Pero la página es español-first: meter preguntas de FAQ en inglés crudo da señal de idioma mixto y mala UX. Agregar "(Deep Cleansing Facial)" al `<title>` lo empujaba de ~53c a ~76c → Google trunca el SERP en ~60c y cortaría justo el término que se quería visible: mala práctica, descartado (no se ofreció como opción). El término rinde igual en `description` (se muestra completo), en `alternateName` (señal estructurada de equivalencia de entidad para Google/LLMs) y en un ítem de FAQ español honesto. 4 capas coherentes (meta, schema, contenido, llms.txt) valen más que dejar `llms.txt` desalineado.
**How to apply:** Términos EN de servicios español-first → `description`/`alternateName`/FAQ-puente en español, nunca reemplazando copy español ni en `<title>` si lo alarga más allá de ~60c. Cuando se agrega/quita un ítem de FAQ de una landing, actualizar el `count` en `tests/faq-consistency.spec.js` (asserta `FAQPage.mainEntity.length`). Spec: `docs/superpowers/specs/2026-08-28-deep-cleansing-facial-en-term-design.md`.

## 2026-08-28 — Track A #2: sub-tag de hero canónico `West Palm Beach, FL` en fuente única
**Decision:** El sub-tag de ubicación del hero (el `<span class="localTag">` dentro del `<h1>`, y su equivalente inline en el `Hero` de Home) se estandariza a `Medical Spa · West Palm Beach, FL` y se centraliza en `src/data/siteMeta.js` (`export const HERO_LOCAL_TAG`). Los ~10 call sites (literales en `LandingPage.jsx`, `TreatmentDetailPage.jsx`, `Hero.jsx`; datos en `categoryPages.js` ×6 y `aboutPage.js`) importan la constante. NO se toca: prosa/meta descriptions que escriben "West Palm Beach, Florida" (lenguaje natural, correcto), footer address, `organizationSchema` `addressLocality`/`areaServed` City `name` (el schema quiere la ciudad pura), y `/nancy-nieto` (sin sub-tag por decisión previa: H1 = nombre propio). Este ciclo es solo el sub-tag del hero, no una migración "Florida→FL" global.
**Why:** El ítem venía del findings doc §S5.2 ("competidores ponen la ciudad en el H1, Derma.M usa una línea aparte"), pero el audit mostró que `localTag` ya se renderiza DENTRO del `<h1>` (`PageHero.jsx:70-73`) — el intent SEO ya estaba cumplido. Lo que quedaba era una inconsistencia de NAP: el hero decía "West Palm Beach" a secas mientras el `<title>` de Home, el address del GBP y el footer dicen "West Palm Beach, FL". Best practice de local SEO = una sola forma canónica de la referencia de ubicación en sitio + GBP + citations; "FL" (abreviatura de 2 letras) es la convención US y la que ya usa el `<title>`. Beneficio por vector: **SEO** refuerza la asociación entidad↔ubicación en ~35 páginas y limpia señal diluida; **AEO** sube la confianza de extracción del par negocio↔ciudad+estado en LLMs (una sola forma en H1 + title de cada página); **GEO** el hero pasa a hablar el formato de token de estado que usan los directorios y el grafo local. La constante compartida elimina la clase de bug "drift entre heroes" que fue la causa raíz de que este ítem existiera (un fix previo se aplicó en un call site y los otros quedaron atrás).
**How to apply:** Cualquier string que deba estar sincronizado entre varios heroes va a `src/data/siteMeta.js`, nunca inline. Al agregar un hero nuevo, importar `HERO_LOCAL_TAG` — no re-teclear el literal. `test:visual`: el cambio de ", FL" cayó dentro de la tolerancia del 2% (`maxDiffPixelRatio: 0.02`), 34/34 sin diffs, sin re-baseline. Spec: `docs/superpowers/specs/2026-08-28-hero-localtag-city-state-consistency-design.md`.

## 2026-08-28 — Track A #3: "(PRF)" en superficies de contenido + reescritura de la FAQ del PRF landing
**Decision:** (A) La forma canónica pasa a "Plasma Rico en Plaquetas y Fibrina (PRF)" — el paréntesis "(PRF)" se agrega SOLO en superficies de contenido/structured data: `<title>`/`og:title`/`twitter:title`, H1 (`hero.title` en `landingPages.js`), `Service` schema `name` (+ nuevo `alternateName: ["PRF", "Platelet-Rich Plasma & Fibrin (PRF)"]`), `BreadcrumbList` pos 3, `organizationSchema.knowsAbout` (de "(PRP y PRF)" a "(PRF)"), y `public/llms.txt` línea 39 (forma EN "Platelet-Rich Plasma & Fibrin (PRF)"). Nav ×2, footer, `<h3>` de `FeaturedServices` y título de card en `categoryPages.js` quedan con el nombre ES completo LIMPIO, sin paréntesis. "PRPF" verificado ausente de `src/`+`llms.txt`. "PRP" pelado se mantiene solo en las 2 FAQ explicativas y el disclaimer legal. (B) La FAQ del PRF landing se reescribió 7→8 ítems: voz de búsqueda, respuesta directa en la 1ª frase, cada Q→A autocontenida; se fusionaron los 2 ítems casi-duplicados de comparación; el #1 compuesto se dividió; 3 ítems nuevos de intención alta (rellenos/fillers, duración de resultados, efectos secundarios) derivados de las PAA del findings doc §189; 1ª mención del bloque con la forma canónica completa. `faq-consistency.spec.js` count `/prf-y-fibrina` 7→8.
**Why:** Research Apify (findings doc §S1, lectura de SERP por término): "PRPF" es inparseable (SERP basura), "PRP" pelado devuelve mercado ortopédico/medicina deportiva (no el nuestro), "PRF" solo tiene cancha vacía a nivel local, y el término ES completo ya lo gana Derma.M (único resultado sin geo). El "(PRF)" captura al buscador de sigla sin diluir el término ES. Se pone solo en contenido/schema y no en el chrome porque ahí el anchor text ya carga la entidad con el nombre completo y el paréntesis en un `<li>` es ruido visual por ganancia marginal (coherente con la decisión del 2026-08-27). La FAQ vieja tenía redundancia (3 ítems repetían "combinamos PRP+PRF tópico con Dermapen"), preguntas en voz de marca ("¿Cuándo se aprecian los resultados finales de vitalidad?") en vez de voz de búsqueda, y no cubría intenciones que las PAA muestran como reales (vs fillers, duración, efectos secundarios). Best practice AEO: los LLMs extraen pares Q→A sueltos → cada respuesta debe ser autocontenida y directa. Nota: Google retiró el rich result de FAQ para casi todos los sitios → el valor del `FAQPage` schema hoy es AEO/LLM + relevancia on-page + alineación PAA, no estrellas.
**How to apply:** Mención nueva del tratamiento como nombre propio en contenido/schema → "Plasma Rico en Plaquetas y Fibrina (PRF)". En nav/footer/anchor text → nombre ES completo sin paréntesis. Nunca "PRPF"; "PRP" pelado solo en prosa que explica la diferencia técnica. Al reescribir cualquier FAQ de landing: voz de búsqueda en la pregunta, respuesta directa 1ª frase autocontenida, 1 pregunta por ítem, sin claims nuevos (derivar de copy vetada), sin banned words/garantías, y actualizar el `count` en `tests/faq-consistency.spec.js`. Spec: `docs/superpowers/specs/2026-08-28-prf-canonical-naming-design.md`.

## 2026-08-28 — FAQ #4 pág. 1: método de captura PAA (sin Apify) + append-only + sin ítem de precio
**Decision:** El ítem #4 de la cola ("FAQ sembradas desde PAA reales") para `/limpieza-facial-profunda` se ejecuta SIN un ciclo previo de captura vía Apify (el "4a" que estaba planteado en `NEXT.md`). Insumo: los seeds PAA en inglés ya presentes en el findings doc §189 + un pase manual de ~15 min con `WebSearch` para confirmar el fraseo español real. Se agregan 3 ítems (6 → 9), **append-only** — los 6 ítems existentes no se tocan ni se reordenan. **Sin ítem de precio** ("¿cuánto cuesta?") pese a ser PAA real. Copy derivada de `problem.body`/`benefits`/`quickFacts`/`howItWorks` ya vetados, cierre en tono relacional (§S7).
**Why:** `/llm-council` (5 asesores + peer review, 2026-08-28): 4/5 contra el ciclo Apify autónomo. Razones convergentes: (1) los seeds EN ya existen y son usables — el cuello de botella real es el filtro de compliance + el count exacto del `faq-consistency.spec.js`, que un scrape no toca; (2) `languageCode=es` + `countryCode=us` + WPB probablemente devuelve PAA degradado/vacío → se gastaría el ciclo para caer igual al fallback de web search; (3) un ciclo sin cambios en el sitio no "se gana su ceremonia" (PROGRESS/DECISIONS/NEXT para un append a un doc) y sienta el precedente de que toda tarea de contenido "necesita" un run de Apify. Append-only: los 6 existentes ya están en voz de búsqueda y limpios de compliance; el valor está en cubrir las PAA que faltan, no en repulir lo que funciona (regla de blast-radius de `CLAUDE.md`). Sin precio: el sitio no publica precios en ninguna página; un Q&A cuya respuesta es "no te lo decimos acá" resta más de lo que suma en AEO y rompe la consistencia de "precio solo en valoración". La query de precio la captura el snippet de la página, no necesita ítem dedicado. Nota (heredada del ciclo #3): Google retiró el rich result de FAQ para casi todos los sitios → el valor del `FAQPage` schema hoy es AEO/LLM + relevancia on-page + alineación PAA, no estrellas en el SERP.
**How to apply:** Para el ítem #4 restante (`/tratamientos-postoperatorios`, próximo ciclo): mismo método — seeds §189 + pase manual `WebSearch` ES, sin Apify; evaluar append-only salvo que la FAQ actual tenga problemas reales de compliance/voz. Cuando una landing no publica precio, no crear ítem de FAQ de precio. Al agregar/quitar ítems de FAQ: actualizar el `count` en `tests/faq-consistency.spec.js` en la misma commit; correr `test:visual` (la FAQ no suele estar en el snapshot de la landing → normalmente sin re-baseline). El ítem "Estrategia PRF/postop demand-gen" del Expansionist queda como su propio ítem de cola con brainstorming (ya es el #6/#7 de `NEXT.md`), no se cuela como scope-creep acá. Spec: `docs/superpowers/specs/2026-08-28-limpieza-facial-faq-paa-design.md`.

## 2026-08-28 — FAQ #4 pág. 2: ruling de wording de fibrosis en `/tratamientos-postoperatorios` + cierre del ítem #4
**Decision:** En copy postoperatoria (FAQ y en general) se permite "**prevenir / prevención de fibrosis**", "acompañar", "buscan cuidar la textura del tejido", "se asocia con una menor formación de fibrosis y adherencias". Queda **prohibido** "cura / trata / elimina / resuelve la fibrosis" y cualquier claim de complicación médica (seroma, trombosis, infección). Todo ítem que toque el "no hacerse el drenaje" se limita a inflamación / pesadez / endurecimiento del tejido, sin escalar a riesgo de salud, y difiere al cirujano / especialista a cargo. Con esto se cierra el ítem #4 de la cola completo: PRF (ciclo #3 `cb53307`), `/limpieza-facial-profunda` (`b7a6452`, 6→9), `/tratamientos-postoperatorios` (`7d6126d`, 5→9).
**Why:** Instrucción explícita del usuario (2026-08-28): "nada de cura, pero sí prevención de fibrosis por ejemplo". "PREVENCIÓN DE FIBROSIS" ya es un título de beneficio vigente en la página → el término es consistente con copy ya vetada. "Cura" implicaría claim terapéutico sobre una condición → territorio médico/regulatorio (Florida med-spa, FTC substantiation). El drenaje linfático manual estético es acompañamiento, no tratamiento de una patología. El framing "qué pasa si no…" es PAA real y de alto valor AEO, pero convertirlo en advertencia de riesgo de salud sería un claim no sustentado y alarmista.
**How to apply:** Cualquier copy futura de postop / drenaje / recuperación: "prevenir/acompañar/favorecer", nunca "curar/tratar/eliminar" una condición; sin nombrar complicaciones médicas; deferimiento explícito al cirujano donde haya duda de indicación. Specs: `docs/superpowers/specs/2026-08-28-postoperatorios-faq-paa-design.md` (+ pág. 1 `…limpieza-facial-faq-paa-design.md`).

## 2026-08-28 — Ítem #5: la estrategia PRF se consolida en spec, sin tocar el sitio en este ciclo
**Decision:** El ítem #5 de la cola ("estrategia PRF must-win") se cierra escribiendo **solo** `docs/superpowers/specs/2026-08-28-prf-content-strategy-design.md` — documento interno, cero cambios en `src/`/schema/protegidos/baselines. Decisiones que fija la spec: (1) **arquitectura de contenido = enriquecer el landing `/prf-y-fibrina` existente** (secciones comparativas vs PRP / vs fillers / vs PDGF·under-eye / duración + tabla) **+ expandir FAQ**, **sin ruta nueva**; guía pilar y página EN quedan *condicionales* (solo si el landing enriquecido rankea / si el tráfico EN de ads convierte). (2) **Ads = capa especificada, no ejecutada** — el doc define canales (Google Search para captura de intención, Meta para demand-gen real), ángulos, audiencias y listas de términos; ningún ciclo hijo crea campañas ni invoca el MCP de Meta Ads; el gasto es decisión del negocio. (3) El slug `/prf-y-fibrina` **no se toca** (ratificado, decisión 2026-08-27). (4) 4 ciclos hijo en §8 de la spec, cada uno con su propio brainstorm→aprobación→1 cambio; orden: secciones comparativas → FAQ → (condicional) EN → (condicional) pilar.
**Why:** `INTAKE.md:190` decía textual "Buscar la estrategia correcta (no está resuelta)": el research de 3 días (whitespace local confirmado, término nacional EN real, cluster comparativo vivo, tiering empírico por 130 reseñas) era insumo, no estrategia. Consolidarlo en una spec evita re-derivarlo cada sesión. Se elige enriquecer el landing en vez de abrir rutas porque el proyecto está near-final (blast radius: una ruta nueva = ruta+template+schema+internal-linking+compliance+baseline = XL) y porque una sola página autoritativa concentra señal de ranking mejor que varias delgadas. Los ads van en el doc pero no se ejecutan porque el gasto y la operación de campañas no son código y no son decisión del agente; además la demanda PRF es hoy nacional/EN mientras la cancha vacía es el SERP local ES → sin ads el funnel no se llena, pero eso es una decisión de negocio con su propio timing.
**How to apply:** Próximo trabajo PRF = ciclo hijo #1 de la spec (enriquecer `landingPages.js` `prfYFibrina` con secciones comparativas; copy derivada de `whatIsBody`/`benefits`/`howItWorks` vetados, compliance vs `MEDICAL_COMPLIANCE.md`, posible re-baseline del snapshot del landing). No abrir ruta EN ni pilar sin que se cumpla su condición. Cualquier término/ángulo de ads nuevo se agrega a la spec, no se implementa. Spec: `docs/superpowers/specs/2026-08-28-prf-content-strategy-design.md`.

## 2026-08-29 — Ítem #5b: FAQ comparativa del PRF landing — ruling "evolución sí, superioridad no" + #5a descartado
**Decision:** (A) En copy del PRF se permite posicionar el PRF como **evolución del PRP por mecanismo** ("matriz de fibrina → liberación sostenida y progresiva de factores de crecimiento, en lugar de una única liberación inicial como el PRP") — consistente con el ítem 2 ya vetado de la página. Queda **prohibido**: "mucho mejor" o cualquier veredicto de superioridad en el body de la respuesta (la palabra "mejor" solo puede aparecer en la *pregunta*), y "los resultados duran más" o cualquier claim de longevidad de resultado (contradice el framing de sesiones de mantenimiento de la propia página). (B) El ítem #5a de la spec de estrategia PRF (`2026-08-28-prf-content-strategy-design.md` §8.1 — secciones comparativas vs PRP/fillers/PDGF·under-eye + **tabla comparativa**) se **descarta**. Lo que se ejecuta del PRF-content es solo la expansión de FAQ (§8.2). (C) Se agregan 4 ítems (8 → 12), append-only; `faq-consistency.spec.js` count `/prf-y-fibrina` 8 → 12. Sin contenido PDGF (sin fuente vetada). Sin ángulo geográfico "West Palm Beach" en las respuestas.
**Why:** (A) El `Claims Framework` de `MEDICAL_COMPLIANCE.md` exige "Head-to-head RCT or equivalent" para claims de superioridad comparativa (FTC + Florida) — mismo criterio regulatorio que sacó el dental del GBP. "Evolución por mecanismo" describe el linaje tecnológico (el PRF es la generación derivada del PRP) y una propiedad de la matriz de fibrina ya afirmada en la página, no un veredicto de eficacia. "Duran más" es un claim de outcome no sustentado y además incoherente con la copy vigente. El usuario pidió explícitamente reforzar el PRF ("el PRF es mucho mejor porque la mejora es gradual y dura más") y se hizo push back por nombre: se le dio el posicionamiento-como-upgrade sin cruzar la línea. (B) La FAQ ya carga la intención comparativa (los ítems 2/3/4 vigentes cubren PRP/PRP-facial/fillers). Una tabla médica "X vs Y" requería un tipo de sección nuevo en `LandingPage` + un baseline visual nuevo + exponía justo el claim de superioridad que (A) prohíbe — blast radius alto para un sitio near-final, ganancia marginal sobre Q→A bien formadas para AEO. (C) PDGF: la spec lo listaba pero no hay copy vetada que lo describa → inventar. Geo en respuestas: el landing ya da la señal local por `LocalBusiness` schema + `HERO_LOCAL_TAG` + H1; forzar "West Palm Beach" en una respuesta lee como keyword stuffing y ligar el tratamiento a "daño solar / clima de Florida" roza un claim de estructura/función.
**How to apply:** Copy futura de PRF/PRP: "evolución respecto al PRP", "liberación sostenida/prolongada" (mecanismo) — nunca "mejor/más efectivo/superior" como claim, nunca "los resultados duran más". Para cualquier tratamiento con un comparador (X vs Y): preferir ítems de FAQ Q→A autocontenidos antes que una tabla comparativa (menor blast radius, mismo valor AEO, menos exposición a claim de superioridad). No introducir un término/entidad (PDGF, foto-daño, clima) sin fuente en copy vetada. La señal local va por schema/H1/hero-tag, no por menciones forzadas en FAQ. Al agregar/quitar ítems de FAQ: actualizar el `count` en `tests/faq-consistency.spec.js` en la misma commit. Specs/plan: `docs/superpowers/{specs,plans}/2026-08-29-prf-landing-faq-comparative-cluster*`.

## 2026-08-29 — Ítem #6: cuña postop demand-gen — FAQ on-site derivada de videos de ads + GBP/ads especificado
**Decision:** (A) El ítem #6 de la cola se ejecuta como **3 ítems de FAQ** en `/tratamientos-postoperatorios` (9 → 12, append-only), derivados de 2 transcripts de video de ads de la clínica (`D:\Derma_Content\02-FacebookAds\1_Videos\29082026\POST-OP\`) como fuente vetada. `faq-consistency.spec.js` count 9 → 12. (B) El número de sesiones se redacta como **dos fases** — activa intensiva (el ítem existente "10 a 15 sesiones, 2–3/semana" **no se toca**) + mantenimiento espaciado (1×/mes o cada mes y medio durante varios meses, según el cirujano). Evita contradecir la cifra ya vetada aunque el video diga "mínimo 20". (C) Los **posts de GBP + copy base de Facebook ads** se especifican en el spec pero **no se ejecutan** — van a sesión Track B con la dueña logueada en GBP, mismo patrón que los ads de PRF. (D) La pieza/sección "por qué el postoperatorio importa" se **difiere** a ciclo propio, condicional a que la landing gane tracción.
**Why:** (B) Dos números distintos de sesiones en la misma página = incoherencia que un crawler de IA y un lector detectan; el video es la voz de marketing más nueva pero el ítem viejo ya pasó compliance y está vivo — reconciliarlo es un cambio extra fuera del alcance "append 3 ítems". El marco de dos fases es lo que el propio video describe (fase de drenaje activo + fase de reparación con controles espaciados), así que no hay que elegir. (C) Publicar en GBP requiere a la dueña logueada y aprobación por post; no es código ni decisión del agente. (A) La cuña demand-gen (sur de FL: mucha cirugía, pocos proveedores de postop, cirujanos que no lo prescriben — `COMPETENCIA-SERVICIOS-2026.md` §S7) se ataca on-site respondiendo la objeción que frena la reserva ("¿me hace falta si mi cirujano no lo dijo?", "¿no bastan 6-8 sesiones?"), no abriendo una sección nueva. Compliance: "prevenir endurecimientos/fibrosis/adherencias" + deferimiento al cirujano ya está permitido (DECISIONS 2026-08-28); "seromas" como gancho de miedo y "no pongas tu inversión en riesgo" son del video pero se quedan fuera del sitio — territorio de paid social, criterio de la clínica.
**How to apply:** Copy futura de postop: marco de dos fases (activa + mantenimiento), nunca un segundo número de sesiones que contradiga el ítem vigente sin reconciliarlo en el mismo ciclo. Transcripts de video/ads de la clínica = fuente vetada válida para derivar FAQ/GBP, siempre con cruce contra `MEDICAL_COMPLIANCE.md` y el ruling de postop antes de publicar. Contenido de GBP/ads se especifica en el spec, no se ejecuta desde el agente. Spec: `docs/superpowers/specs/2026-08-29-postop-demand-gen-wedge-design.md`.

## 2026-08-29 — "Mejores med spa de WPB": no hay página nueva; se resuelve como 1 FAQ en `/contacto`
**Decision:** El ítem #1 de la cola de `NEXT.md` ("página mejores med spa de West Palm Beach" — ruta+template+copy+schema+internal-linking+compliance+baseline, [XL]) se **descarta**. En su lugar se agrega **1 ítem de FAQ** a `/contacto` (`contactFaq.items`, 5 → 6; `faq-consistency.spec.js` count 5 → 6): "¿Cómo elijo un buen med spa en West Palm Beach?", redactado como criterios de decisión (licencia + quién ejecuta cada tratamiento + valoración previa + explicación en tu idioma) con Derma.M como respuesta vía hechos verificables (bilingüe, credenciales de Nancy verbatim de `aboutPage.js`) — **sin claim de superioridad** ("mejor/#1/líder" solo puede aparecer en la pregunta, nunca en el body). Host = `/contacto` porque es la única de Home/`/nosotros`/`/contacto` con FAQ ya cableada a un data file; en Home o `/nosotros` habría que construir la sección FAQ (sale de XS).
**Why:** El origen del ítem era posicionar vía un premio (BusinessRate.com "Best of 2026"), ya descartado 2026-08-27 (`INTAKE.md:66` — pay-to-display, riesgo FTC de badge de pago presentado como logro, choca con `MEDICAL_COMPLIANCE.md`, resta credibilidad al trabajo de entidad/E-E-A-T). Sin el premio, la idea mutó en "página nueva para el término", que salió como candidata en research (`COMPETENCIA-SERVICIOS-2026.md` §S5.5) pero nunca se aprobó. Impacto real de esa página: map pack nulo (lo mueve el GBP), orgánico cabeza bajo (el término lo dominan directorios/listicles de terceros por diseño — Yelp Top-10, blog de Beverly Hills), solo AEO/long-tail modesto — a cambio de esfuerzo XL en un sitio near-final. La palanca del término es **off-site**: reclamar/optimizar Yelp (lo hace Nancy, bloqueado) + velocidad de captación de reseñas en GBP — ya registrada en `NEXT.md` Bloqueado/backlog. Una FAQ Q→A captura el upside que sí existe (extracción/citación por LLMs + variante navegacional) a costo XS y sin riesgo de compliance/disparagement del formato comparativo tipo listicle.
**How to apply:** No resucitar la "página mejores med spa de WPB" sin (a) un "best of" de **fuente real** (revista/cámara/prensa de Palm Beach — no pay-to-display) o (b) evidencia de que el término convierte y la FAQ no alcanza. Para posicionar en queries "best/mejor X" en general: FAQ Q→A de criterios de decisión (responde la decisión, no el superlativo) + señal de entidad/E-E-A-T + palanca off-site (GBP/reseñas/citaciones), no una landing autopromocional. Replicar esta FAQ en Home/`/nosotros` requiere construir la sección FAQ primero → ciclo aparte. Spec: `docs/superpowers/specs/2026-08-29-contacto-faq-med-spa-eleccion-design.md`.

## 2026-08-29 — Skill `add-media`: proceso de integración de media como skill que envuelve `assets-optimizer`
**Decision:** El pedido "definir el proceso para agregar imágenes/videos y convertirlo en skill" se materializa como **una** skill project-local `add-media` (`.agents/skills/add-media/SKILL.md` + copia sincronizada en `.claude/skills/`), SKILL.md en inglés. Tres procedimientos en un solo archivo: **Acción A** rellenar slot existente (ruta en `src/data/*.js` sin archivo en disco), **Acción B** slot nuevo (precedido de gate `superpowers:brainstorming` + aprobación), **Acción C** video de fondo de hero. La skill **llama** los scripts de `assets-optimizer` (`optimize.js`, `generate-webp.js`) como sub-paso; no los duplica ni los reemplaza. Handoff de archivos crudos = `scratchpad/media-in/`. Nombres genéricos preexistentes (`hero.jpg`) se **mantienen** por defecto (evitar churn de datos); renombre solo a pedido.
**Why:** `assets-optimizer` cubre solo el paso de compresión; no había procedimiento escrito para las otras 8–9 acciones (localizar el slot, nombrar SEO, webp hermano, alt en español trazable, verificar render sin fallback `og-default`, regresión visual con re-baseline, WCAG, ritual de cierre). Un solo archivo con 3 acciones (vs. 3 skills) porque comparten ~80% de los pasos y el mismo checklist de DoD — separarlas duplicaría el mantenimiento. Wrap en vez de fork de `assets-optimizer` para no tener dos fuentes de verdad de los parámetros de compresión (CRF 28, `-an`, <200KB, q80 webp). "En cada acción" del pedido → las 3 acciones son los tres modos en que aparece media faltante en este sitio (slot vacío / sección sin slot / hero video).
**How to apply:** Cada imagen/video = un ciclo (gate de una-cosa-por-ciclo de CLAUDE.md). Invocar `add-media` cuando el usuario quiera agregar/reemplazar media. Primeros candidatos: 8 slots de `/nosotros` (`about/hero.jpg` + 6 `.jpg` de equipo + 2 `.mp4`). Spec: `docs/superpowers/specs/2026-08-29-add-media-skill-design.md`. Editar SKILL.md = editar **ambas** copias (`.agents/` y `.claude/`).

## 2026-08-29 — `/nancy-nieto` Academy: patrón spotlight con imagen a la derecha, `min-height` en `.spotlightRow`, y NO link en navbar
**Decision:** (A) La sección 4 de `FounderBioPage` ("DERMA.M y DERMA.M Academy") se reconstruye con el **mismo patrón `.spotlight*` de la sección "Historia"** (panel de texto + imagen a sangre completa), no con un grid contenido. Espejada: imagen a la **derecha** (Historia la tiene a la izquierda). (B) Lleva un **link saliente seguido** "Conoce DERMA.M Academy ↗" a `https://dermamacademy.com` (`target=_blank rel="noopener noreferrer"`, sin `nofollow`); data en `dermamYAcademy` (`aboutPage.js`) vía `href` + `linkLabel`. (C) `.spotlightRow` en desktop pasa de `height: clamp(680px,44vw,800px)` a `min-height:` la misma expresión — la fila crece hasta encajar el texto en vez de clipearlo. Aplica a las secciones 2 y 4 (únicas que usan `.spotlightRow`). (D) **No** se agrega link a `/nosotros/nancy-nieto` en el navbar.
**Why:** (A) El usuario pidió explícitamente replicar el formato de "Historia" ("replica las dimensiones de este contenedor... para que queden bien ajustados los textos"); el grid contenido que se intentó primero se descartó por pedido suyo. La spec original decía "no copiar el full-bleed" — el pedido del usuario lo revierte. (B) Un segundo link seguido con ancla "DERMA.M Academy" (además del planeado en `/nosotros`) refuerza que med spa y academy son entidades relacionadas pero distintas — problema de ambigüedad activo en el SERP (audit 8.19). `noreferrer` no implica `nofollow`: el link pasa equity. (C) Con `height` fijo, los 2 párrafos largos de "Historia" (y el link extra de Academy) se cortaban contra los 800px. `min-height` mantiene el piso visual y deja crecer; la imagen estira con `align-items: stretch`. (D) Impacto de un link sitewide en navbar: crawl/PageRank positivo-bajo (Google descuenta navegación boilerplate), E-E-A-T positivo-blando (no es factor directo), AEO marginal — a cambio de diluir un navbar de 3 ítems con una página hoja que ya es hija natural de "Nosotros", y de tocar un componente sitewide casi final (riesgo de regresión visual: el hero de Home captura el navbar). La brecha real de `/nosotros/nancy-nieto` es orfandad: **1** link entrante (desde `/nosotros`), **ausente de `sitemap.xml`**. Eso se arregla con (1) entrada en `sitemap.xml` — archivo protegido, va en la tanda del deploy a Hostinger — y (2) un link contextual in-content desde Home (bloque de fundadora), que vale más para ranking que uno de nav y tiene blast radius local.
**How to apply:** Para promover una página hoja de bio/autor: no meterla en el navbar global; arreglar orfandad vía sitemap + links contextuales desde páginas de alto tráfico. Cualquier fila tipo "panel + media" en `FounderBioPage` usa `.spotlightRow` con `min-height` (nunca `height` fijo) para no clipear texto. Links salientes a `dermamacademy.com` = seguidos, con ancla explícita "DERMA.M Academy", `rel="noopener noreferrer"`. La imagen de estas filas no es link (evita doble tab stop). Spec/plan: `docs/superpowers/{specs,plans}/2026-08-29-nancy-academy-two-column*`.

## 2026-08-29 — Home CTA usa la misma caja que `shared/FinalCTA`; se elimina la línea de teléfono del CTA
**Decision:** (A) `sections/FinalCTA.module.css` (el CTA propio del Home, único consumidor) alinea su `.container` a `shared/FinalCTA`: fuera `min-height: 75vh` y el override `padding: 120px 64px` del `@media (min-width:1024px)`; queda `padding: clamp(80px,12vw,160px) clamp(24px,4vw,64px)` — idéntico a la suma de `shared/.cta` (`clamp(80px,12vw,160px) 0`) + `shared/.container` (`0 clamp(24px,4vw,64px)`). No se tocó tipografía, imagen, filtro, eyebrow ni botones (alcance reducido a pedido explícito del usuario: "ajusta el puto contenedor primero no toques nada mas"). (B) Se elimina la línea `supportingInfo` del CTA del Home ("West Palm Beach · 561 253 5384", con `tel:` link) — JSX + su regla CSS. (C) **No** se unifican los dos componentes `FinalCTA` (`sections/` solo Home, `shared/` los otros 6 tipos de página) en este ciclo.
**Why:** (A) El `min-height: 75vh` estiraba la sección del Home a ~990px vs ~811px del resto; como el fondo es `object-fit: cover`, la misma imagen (1920×900, idéntico archivo en `home/` y en los landings) se escalaba más → se veía "más grande / zoomeada". Igualar la caja da ritmo visual consistente en todos los CTA sin tocar contenido. Home CTA pasó a 763px tras quitar también la línea de teléfono. (B) El usuario la pidió fuera; el teléfono ya está en el navbar (top bar), en Contacto y en el footer, así que no se pierde el dato. (C) Unificar en un componente obligaría a portar a `shared` la animación de zoom-in de la imagen y la línea de teléfono (o descartarlas), y `shared` lo consumen 6 tipos de página → blast radius alto para un sitio near-final; la alineación de CSS captura el 100% del objetivo de consistencia con riesgo local al Home.
**How to apply:** Cualquier CTA nuevo o ajuste de CTA usa el box model de `shared/FinalCTA` (`padding: clamp(80px,12vw,160px) …`, sin `min-height` de viewport). No reintroducir `min-height: NNvh` en secciones con imagen de fondo `cover` — escala la imagen de forma inconsistente. Si en algún momento se decide unificar los dos `FinalCTA`, es su propio proyecto (spec) por el blast radius de `shared`. `test:visual` no cubre el CTA del Home (below-the-fold en los snapshots de Home) — verificar a ojo en `:3000`.

## 2026-08-29 — Auditoría del footer (hallazgos, sin cambios de código)
**Decision:** Auditado el contenido de `src/components/layout/Footer/Footer.jsx` contra el NAP canónico (`organizationSchema.js`, `Contacto.jsx`), `PRODUCT.md`, `MEDICAL_COMPLIANCE.md` y las memorias de posicionamiento. **No se cambió nada** — los hallazgos quedan como cola de pulido en `NEXT.md`. OK: NAP exacto (nombre `DERMA.M, LLC`, dir `5707 S Dixie Hwy UNIT D…33405`, tel `561 253 5384`, email), Aviso Clínico ES/EN compliant, estructura accesible. Hallazgos, por prioridad: (1) **código muerto** — `import { Instagram, Facebook }` + `.socialBlock` CSS presentes pero **sin render** → el footer no muestra ninguna red social pese a `sameAs` (IG/FB/Yelp); (2) **falta el horario** (Contacto lo tiene: "Lun-Sáb 9:00–17:00 · Dom 9:00–13:00"); (3) **bloque legal incompleto** — bottom bar solo enlaza Privacidad + Términos; existen y no se enlazan `/accessibility`, `/treatment-disclaimer`, `/booking-cancellation-refund-policy`, `/legal` (el hub que los agrupa); (4) **deriva de etiqueta de entidad** — el blurb dice "centro de estética, belleza y salud clínica"; en todo el resto del sitio el término es "med spa / Medical Spa" (hero tag, `knowsAbout`, `PRODUCT.md`); (5) "Tratamientos" (col. Navegación) apunta a `/faciales` — label→destino incoherente; (6) col. "Tratamientos" lista 3 landings + 3 de 6 hubs (falta IV Therapy y Capilar; Dental se omite y eso **sí** es coherente con la pregunta abierta de riesgo regulatorio dental); (7) `© 2026` hardcodeado.
**Why:** El usuario pidió auditar si el footer "está bien, hace sentido, está alineado con lo que queremos". La entidad de marca (item 8.19: desambiguar Derma.M / Derma M Academy / Institute) se apoya en usar el mismo término y las mismas señales (`sameAs`) de forma consistente — el footer diverge en ambas. Horario y links legales completos son estándar de local business / postura ADA. Nada de esto bloquea nada; el sitio está near-final y el footer es un componente sitewide (riesgo de regresión visual: el footer entra en varios snapshots) → cada fix es su propio ciclo con brainstorm+aprobación.
**How to apply:** Prioridad de los fixes: 1 (código muerto + redes) → 3 (legal/accessibility) → 4 (etiqueta "med spa"). 2/5/6/7 agrupables en un ciclo de pulido. Al tocar el footer: `test:visual` completo + revisar los snapshots que lo incluyen. La omisión del hub Dental en el footer se mantiene hasta que se resuelva la pregunta dental (NEXT.md §Bloqueado).

## 2026-08-29 — `.spotlightTitle` de `FounderBioPage` baja a 44px en desktop
**Decision:** El `<h2>` de las secciones "Historia" y "Academy" de `/nancy-nieto` (clase compartida `.spotlightTitle`) baja de `56px` a `44px` en `@media (min-width:1024px)`. Base 40px (mobile/tablet) se mantiene. No se usó `clamp()` — cambio de una línea en el valor fijo del breakpoint que ya existía.
**Why:** El usuario observó que el H2 se veía "muy grande" y rompía en 4 líneas. A 56px la diferencia con el `<h1>` del hero (64px) era ~8px = jerarquía casi nula, y los títulos largos ("UNA PROFESIONAL FORMADA EN DOS PAÍSES", "UN ESPACIO PARA CUIDAR Y UN PROYECTO PARA FORMAR") se apilaban en 4 líneas dentro de la columna de texto angosta (~350–480px) del patrón spotlight. 44px da salto 64→44 legible y baja "Historia" a 3 líneas. Se descartó `clamp()` por YAGNI: el único breakpoint problemático era ≥1024px y el valor base ya estaba bien.
**How to apply:** Títulos de sección en plantillas con columna de texto angosta (patrón spotlight / paneles al 50%): mantener el H2 en ~44px máx en desktop, no escalarlo hacia el tamaño del H1. `.spotlightTitle` la comparten Historia y Academy en `FounderBioPage` — cambiarla afecta ambas (deseado, quedan consistentes). El snapshot `nancy-nieto` de `test:visual` solo captura el hero (above-the-fold) → cambios en estos H2 no generan diff; verificar a ojo en `:3000`.

## 2026-08-29 — Labels de CTA de hero: `/nosotros` y `/nancy-nieto` se alinean al estándar `RESERVAR` / `WHATSAPP`
**Decision:** Los hero de `/nosotros` (`aboutPage.js` `hero`) y `/nancy-nieto` (`founderBioPage.hero`) pasan de `primaryCta: "AGENDAR VALORACIÓN"` / `secondaryCta: "HABLAR POR WHATSAPP"` a `"RESERVAR"` / `"WHATSAPP"` — mismos strings que usan los 6 hubs + 3 landings + detalles de tratamiento. **Se mantienen los 2 botones** (no se elimina el primario de reserva). Cambio solo en `src/data/aboutPage.js`; `PageHero` y su CSS no se tocan. Los CTA de las secciones `FinalCTA` de esas páginas ("AGENDAR VALORACIÓN" / "CONSULTAR POR WHATSAPP") **no** se cambian — es otro componente.
**Why:** El usuario reportó que el botón del hero de `/nosotros` se veía más grande que en los demás hero y pidió consistencia. Causa real: `.primaryCta`/`.secondaryCta` de `PageHero` tienen padding idéntico en todos lados; lo que ensanchaba el botón era el largo del texto ("HABLAR POR WHATSAPP" vs "WHATSAPP"). Esos 2 eran los únicos hero con labels verbosos. Se evaluó "dejar solo WhatsApp" (lo que pidió primero el usuario) y se descartó: quitar el CTA de reserva de dos páginas es una regresión de conversión (CRO: el hero mantiene una acción primaria), y dejar esos hero con 1 botón sería *otra* inconsistencia frente a los 10+ hero de 2 botones. El usuario delegó la decisión ("no lo sé, revísalo").
**How to apply:** Todo hero nuevo o editado usa `primaryCta: "RESERVAR"` + `secondaryCta: "WHATSAPP"` (o el subset que aplique). No introducir labels de CTA verbosos en `PageHero` — rompen la consistencia de ancho. Si en el futuro se quiere un tono más consultivo en el CTA de About/founder, es decisión de copy con su propio gate, no un fix de consistencia.

## 2026-08-29 — Bug: los botones del hero del Home no hacen nada (detectado, fix en próximo ciclo)
**Decision:** Registrado como bug a arreglar en ciclo propio. En `src/components/sections/Hero/Hero.jsx` (hero del Home) los CTA "Reservar" y "Contacto" son `<button>` sin `onClick`, sin `href` y sin `<Link>`/`<a>` envolvente → no navegan a ningún lado. Todos los demás hero (`PageHero`) renderizan `<a href={bookingUrl|whatsappUrl} target="_blank">`.
**Why:** Muy probablemente resto del scaffold del export de Google AI Studio que nunca se cableó. Es un fallo funcional real en la página más visitada. No se arregla en el mismo ciclo que el cambio de labels (un cambio por ciclo) pero se documenta para no perderlo.
**How to apply:** Fix esperado: "Reservar" → `<a href={bookingUrl} target="_blank" rel="noopener noreferrer">` (mismo `bookingUrl` que `PageHero`/`FinalCTA`); "Contacto" → `<Link to="/contacto">` (el label indica intención de ir a la página de contacto, no a WhatsApp). Reusar las clases Tailwind actuales del botón. `test:visual` — el hero del Home sí está en el snapshot `home-hero`, pero cambiar `<button>`→`<a>` con el mismo texto/estilo no debería generar diff; verificar.

## 2026-08-29 — Fix: CTAs del hero del Home cableados (`<button>` sin acción → `<a>`/`<Link>`)
**Decision:** En `src/components/sections/Hero/Hero.jsx` los dos CTA del hero del Home dejan de ser `<button>` con solo clases y pasan a: "Reservar" → `<a href={bookingUrl} target="_blank" rel="noopener noreferrer">` (`bookingUrl` = `import.meta.env.VITE_SQUARE_BOOKING_URL || <fallback Square>`, el mismo const que definen `PageHero` y `sections/FinalCTA`); "Contacto" → `<Link to="/contacto">` de react-router. Se conservan las clases Tailwind del botón + `no-underline`. Se agregó `import { Link } from 'react-router-dom'`.
**Why:** Los `<button>` no tenían `onClick`, `type`, ni `<a>`/`<form>` — no navegaban a ningún lado. Casi seguro residuo del export de Google AI Studio que nunca se cableó; fallo funcional en la página más visitada. El patrón correcto ya existe en el repo (todos los demás hero via `PageHero` renderizan `<a href={bookingUrl|whatsappUrl} target="_blank">`). "Contacto" apunta a `/contacto` (no a WhatsApp) porque el label lo dice explícitamente; es link interno → `<Link>` para no romper el routing SPA.
**How to apply:** Ningún CTA del sitio debe ser un `<button>` sin handler. Acción de reserva = `<a href={bookingUrl} target="_blank" rel="noopener noreferrer">` con `bookingUrl` desde `VITE_SQUARE_BOOKING_URL` + fallback; navegación interna = `<Link to>`; WhatsApp = `<a href={whatsappUrl} target="_blank">`. Al convertir `<button>`→`<a>` con estilos de botón, agregar `no-underline`. `Hero.jsx` (Home) es el único hero que NO usa `PageHero` — si se toca de nuevo, revisar que los CTA sigan cableados. `test:visual`: `<button>`→`<a>` mismo texto/clase no generó diff (34/34).

## 2026-08-29 — Link de la sección de fundadora del Home: se repunta a la bio (no se agrega un 2º link)
**Decision:** `founderPrimer` en `src/data/aboutPage.js` (usado por `FounderSection` del Home): `linkTo` pasa de `/nosotros` a `/nosotros/nancy-nieto` y `linkLabel` de "Conoce a Nancy y al equipo" a "Conoce a Nancy Nieto". No se deja el link a `/nosotros` ni se agrega un segundo link.
**Why:** Ejecuta el fix de orfandad decidido el 2026-08-29 (`/nosotros/nancy-nieto` tenía 1 solo link entrante, ausente del sitemap). Se repunta en vez de agregar un 2º link porque: (a) la sección `FounderSection` es enteramente sobre Nancy (título "NANCY NIETO", sus credenciales, su filosofía) → su bio es el destino correcto; el "y al equipo" era lo único que justificaba mandar a `/nosotros`; (b) dos links en un bloque chico es clutter; (c) `/nosotros` no pierde nada — sigue en navbar sitewide + footer, no es huérfana, y el equipo está a un click desde ahí. Ancla con nombre completo ("Nancy Nieto" > "Nancy") por la entidad-persona (8.19). El segundo paso del fix (entrada en `sitemap.xml`) sigue atado al deploy a Hostinger (archivo protegido).
**How to apply:** Al enlazar desde un bloque temático (sección de una persona/servicio) preferir el destino más específico y relevante con ancla descriptiva, no la página índice. `/nosotros/nancy-nieto` ahora tiene 2 links internos; sigue faltando la entrada de sitemap (NEXT.md §Bloqueado deploy). Memoria: [[project_nav_orphan_bio_page]].

## 2026-08-29 — `test:visual` se gatea por tipo de cambio (no se corre en cambios data-only)
**Decision:** `npm run test:visual` deja de correrse en todo ciclo. Se corre para: cambios de CSS, ediciones de componente/template compartido, cambios de layout/estructura, o cualquier clase usada en >1 lugar. Se **omite** para: ediciones de data/copy en `src/data/*`, edición de contenido de un solo componente, o cambios demostrablemente fuera de todos los viewports de los snapshots — en esos casos alcanza con verificación en el browser (`:3000`). El cross-check contra `MEDICAL_COMPLIANCE.md` se mantiene siempre. Registrado en `CLAUDE.md` §"Definition of done for visual/structural changes".
**Why:** El usuario señaló que en varios ciclos de esta sesión (labels de CTA en `aboutPage.js`, link de `founderPrimer`) el suite fue 2.7 min de puro overhead: el cambio ya estaba verificado en el browser y encima los snapshots afectados son above-the-fold → ni capturaban el cambio. El valor real de `test:visual` es atrapar regresión colateral en páginas que no estoy mirando y en `mobile-safari` cuando toco algo compartido (CSS, templates) — no aplica a data aislada. CLAUDE.md ya acota el requisito a "cambios visuales/estructurales"; esto lo hace explícito.
**How to apply:** Antes de correr `test:visual` preguntarse: ¿esto toca CSS / un componente o template compartido / una clase reusada / el layout? Sí → correr. Solo data/copy/contenido de 1 componente y ya lo vi en el browser → skip y decirlo en el cierre ("data-only, sin test:visual"). Ante la duda, correr.

## 2026-08-29 — Footer: fila de redes (IG/TikTok/FB) + TikTok añadido a `sameAs`
**Decision:** Se cablea la fila de redes muerta del footer con **3** links en la columna de marca, debajo de la descripción: Instagram, TikTok, Facebook (ese orden, el de la ficha de GBP). Instagram y Facebook usan los componentes de `lucide-react` ya importados; **TikTok va como `<svg>` inline** porque `lucide-react@0.546` no tiene glifo de TikTok (`Tiktok`/`TikTok` no existen). Íconos a 24px (se probó 20px, el usuario los pidió más grandes). Se agrega `https://www.tiktok.com/@derma.m` a `sameAs` en `organizationSchema.js` (antes IG/FB/Yelp) → ciclo de 3 archivos (`Footer.jsx` + `Footer.module.css` + `organizationSchema.js`). Opción "solo borrar el código muerto" (cero UI) se descartó.
**Why:** Los imports `Instagram/Facebook` y `.socialBlock` estaban en el código pero sin render → un footer de med spa sin presencia social es un hueco y contradice el `sameAs` del schema, que ya declara las cuentas. Se agregan las 3 (no 2) porque la ficha de GBP (`INTAKE.md:147`: IG `/dermamskinhealth/`, TikTok `@derma.m`, FB `/DermaMskinhealth`) lista TikTok como cuenta real — fuente vetada, no inventada. TikTok inline en vez de otra librería de íconos para no sumar dependencia (PRODUCT.md: no ampliar el stack); el path es el glifo geométrico estándar, uso nominativo (ícono que enlaza al perfil oficial). Añadir TikTok a `sameAs` en el mismo ciclo porque si no el footer muestra 3 y el schema declara 2 = inconsistencia nueva; `organizationSchema.js` no es archivo protegido.
**How to apply:** Íconos de marca que lucide no tenga → `<svg>` inline monocromo con `fill="currentColor"`, mismo tamaño que los de lucide, `aria-hidden` en el SVG + `aria-label` en el `<a>`. Redes del negocio: la fuente es la ficha de GBP (`INTAKE.md`), y footer + `organizationSchema.js` `sameAs` deben listar lo mismo. El footer no está en ningún snapshot de `test:visual` (todos above-the-fold) — verificar a ojo en `:3000`. Cierra ítem 1a de la auditoría del footer; quedan 1b (horario), 1c (legal/accessibility), 1d (entidad "med spa" + varios).
